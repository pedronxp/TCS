---
phase: 03-ui-auth-agente
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(auth)/login.tsx
autonomous: true
requirements:
  - UX-redesign
must_haves:
  truths:
    - "Error message renders with rgba(239,68,68,0.08) background, alert-circle icon, and border rgba(239,68,68,0.2)"
    - "Login button uses Button variant=primary from design system (no custom buttonDisabled)"
    - "email.trim() is called before signInWithPassword and before isApproved query"
    - "email.trim().toLowerCase() is used in the isApproved verification query"
  artifacts:
    - path: "app/(auth)/login.tsx"
      provides: "Login screen with inline error display, email trimming, and design system button"
  key_links:
    - from: "app/(auth)/login.tsx"
      to: "components/ui/Button"
      via: "import and usage replacing Entrar button"
      pattern: "Button"
    - from: "app/(auth)/login.tsx"
      to: "email.trim()"
      via: "applied in signInWithPassword call and isApproved query"
      pattern: "email\\.trim\\(\\)"
---

<objective>
Improve login screen UX by replacing plain error text with a visible error banner, applying email trimming to prevent accidental whitespace errors, and replacing the submit button with the design system Button component.

Purpose: Login is the most critical auth screen — visible error feedback and trimmed email reduce failed login attempts.
Output: Modified app/(auth)/login.tsx with error banner, email trimming, and design system Button.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ui-auth-agente/03-RESEARCH.md

<interfaces>
Button: variant="primary"|"secondary"|"ghost"|"danger", disabled, loading, onPress, children
<!-- Feather icons: import { Feather } from '@expo/vector-icons' -->
<!-- supabase.auth.signInWithPassword({ email, password }) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Login screen — error banner, email trim, design system button</name>
  <files>app/(auth)/login.tsx</files>
  <read_first>
    - app/(auth)/login.tsx (read BEFORE editing — understand current error rendering, button, handleLogin function, isApproved query)
    - components/ui/index.ts (verify Button export)
  </read_first>
  <action>
  Read the full current file first. Then apply all changes below without breaking existing Supabase auth logic.

  VISUAL CHANGES — ERROR BANNER:
  Replace the existing `<Text style={styles.errorText}>` block with:
  ```tsx
  import { Feather } from '@expo/vector-icons';
  {error !== null && (
    <View style={{
      flexDirection: 'row',
      alignItems: 'center',
      backgroundColor: 'rgba(239,68,68,0.08)',
      borderWidth: 1,
      borderColor: 'rgba(239,68,68,0.2)',
      borderRadius: 12,
      padding: 12,
      gap: 8,
      marginBottom: 12,
    }}>
      <Feather name="alert-circle" size={16} color="#EF4444" />
      <Text style={{ color: '#EF4444', fontSize: 14, flex: 1 }}>{error}</Text>
    </View>
  )}
  ```

  VISUAL CHANGES — BUTTON:
  Replace the "Entrar no Sistema" button (TouchableOpacity or existing Button) with:
  ```tsx
  import { Button } from '../../components/ui';
  <Button
    variant="primary"
    loading={loading}
    onPress={handleLogin}
    disabled={loading}
  >
    Entrar no Sistema
  </Button>
  ```
  Remove the buttonDisabled style from StyleSheet — Button from design system handles opacity internally.

  Add marginTop: -12 to the links "Recuperar credenciais" and "Validar Token" containers.

  FUNCTIONAL CHANGES — EMAIL TRIM:
  In the handleLogin function (or equivalent), update the signInWithPassword call:
  ```typescript
  const { data, error: authError } = await supabase.auth.signInWithPassword({
    email: email.trim(),
    password,
  });
  ```
  Also update the isApproved verification query to use:
  ```typescript
  .eq('email', email.trim().toLowerCase())
  ```
  The existing `setError(null)` at the start of handleLogin is already correct — keep it.
  </action>
  <acceptance_criteria>
  - grep -n "rgba(239,68,68,0.08)" app/(auth)/login.tsx returns a match
  - grep -n "alert-circle" app/(auth)/login.tsx returns a match
  - grep -n "email.trim()" app/(auth)/login.tsx returns a match
  - grep -n "toLowerCase" app/(auth)/login.tsx returns a match
  - grep -n "Button" app/(auth)/login.tsx returns matches for import and usage
  </acceptance_criteria>
  <verify>
    <automated>
    cd /c/Users/User/Desktop/Projeto/app_defasaCivil/app_defesa_civil_expo && grep -n "rgba(239,68,68" "app/(auth)/login.tsx" && grep -n "email.trim()" "app/(auth)/login.tsx" && grep -n "Button" "app/(auth)/login.tsx"
    </automated>
  </verify>
  <done>
  app/(auth)/login.tsx has a colored error banner with alert-circle icon, email is trimmed before Supabase calls, and the submit button uses Button variant="primary" with built-in loading state.
  </done>
</task>

</tasks>

<verification>
grep -n "rgba(239,68,68,0.08)" "app/(auth)/login.tsx"
grep -n "email.trim()" "app/(auth)/login.tsx"
grep -n "toLowerCase" "app/(auth)/login.tsx"
grep -n "Button" "app/(auth)/login.tsx"
</verification>

<success_criteria>
- Error banner with rgba(239,68,68,0.08) background and alert-circle icon
- email.trim() used in signInWithPassword
- email.trim().toLowerCase() used in isApproved query
- Button variant="primary" with loading prop replaces submit button
- buttonDisabled StyleSheet entry removed
</success_criteria>

<output>
After completion, create `.planning/phases/03-ui-auth-agente/03-03-SUMMARY.md`
</output>
