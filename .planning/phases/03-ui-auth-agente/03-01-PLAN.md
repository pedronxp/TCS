---
phase: 03-ui-auth-agente
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/onboarding.tsx
autonomous: true
requirements:
  - UX-redesign
must_haves:
  truths:
    - "scrollEnabled={true} is present in the FlatList — user can swipe slides manually"
    - "onMomentumScrollEnd updates the current dot indicator when swiping"
    - "Button component from components/ui/Button is used for Pular and Proximo/Comecar"
    - "iconCircle is 220x220 and iconInner is 160x160"
    - "title uses fontSize 30, fontWeight 800, letterSpacing -0.8"
  artifacts:
    - path: "app/onboarding.tsx"
      provides: "Onboarding screen with swipeable slides and design system buttons"
  key_links:
    - from: "app/onboarding.tsx"
      to: "components/ui/Button"
      via: "import and usage for Pular and Proximo/Comecar buttons"
      pattern: "Button"
    - from: "app/onboarding.tsx"
      to: "FlatList scrollEnabled"
      via: "prop on FlatList"
      pattern: "scrollEnabled=\\{true\\}"
---

<objective>
Apply visual hierarchy improvements and enable manual swipe on the onboarding screen. Replace TouchableOpacity buttons with the Button component from the Phase 2 design system.

Purpose: Onboarding is the first screen users see — better visual presence and swipeable slides improve first impressions and reduce drop-off.
Output: Modified app/onboarding.tsx with enlarged icons, improved typography, swipeable FlatList, and design system buttons.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ui-auth-agente/03-RESEARCH.md

<interfaces>
<!-- Design system components available from components/ui/index.ts -->
Button: variant="primary"|"secondary"|"ghost"|"danger", disabled, loading, onPress, children
Card: children, style (optional)
Badge: variant (R1|R2|R3|R4|success|warning|error), children, style
EmptyState: icon (Feather name), title, description, actionLabel, onAction
LoadingState: (zero props)
ErrorState: message, onRetry

<!-- Theme tokens via useTheme() → { theme: typeof Colors.light } -->
theme.primary, theme.background, theme.surface, theme.text, theme.textSecondary, theme.border, theme.iconBackground
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Onboarding — swipe + visual hierarchy + design system buttons</name>
  <files>app/onboarding.tsx</files>
  <read_first>
    - app/onboarding.tsx (read BEFORE editing — understand current FlatList structure, button styles, dot logic)
    - components/ui/index.ts (verify Button export name)
  </read_first>
  <action>
  Read the full current file first. Then apply all changes below without breaking existing navigation logic.

  VISUAL CHANGES:
  1. Resize iconCircle: width: 220, height: 220, borderRadius: 110
  2. Resize iconInner: width: 160, height: 160, borderRadius: 80
  3. Add paddingTop: 100 to the slide container (the View inside renderItem)
  4. Update title style: fontSize: 30, fontWeight: '800', letterSpacing: -0.8
  5. Update subtitle style: fontSize: 17, lineHeight: 26
  6. Update iconCircle marginBottom: 60
  7. Update title marginBottom: 20
  8. Update dots gap: 8; active dot width: 28
  9. Update next/start button: height: 64, borderRadius: 20, text fontSize: 18

  FUNCTIONAL CHANGES:
  1. On the FlatList, change scrollEnabled={false} to scrollEnabled={true}
  2. Add onMomentumScrollEnd prop to FlatList:
     ```
     onMomentumScrollEnd={({ nativeEvent }) => {
       const index = Math.round(nativeEvent.contentOffset.x / width);
       setCurrent(index);
     }}
     ```
  3. Replace the TouchableOpacity "Pular" button with:
     ```
     import { Button } from '../../components/ui';
     <Button variant="ghost" onPress={() => router.replace('/(auth)')}>
       Pular
     </Button>
     ```
  4. Replace the TouchableOpacity "Próximo"/"Começar" button with:
     ```
     <Button
       variant="primary"
       onPress={current === slides.length - 1 ? handleFinalizar : handleNext}
     >
       {current === slides.length - 1 ? 'Começar' : 'Próximo'}
     </Button>
     ```
     Where handleFinalizar/handleNext are the existing navigation handlers — do not change their logic.

  Remove the old buttonDisabled or buttonStyle overrides that conflict with the Button component.
  </action>
  <acceptance_criteria>
  - grep -n "scrollEnabled={true}" app/onboarding.tsx returns a match
  - grep -n "onMomentumScrollEnd" app/onboarding.tsx returns a match
  - grep -n "Button" app/onboarding.tsx returns matches for import and usage
  - grep -n "iconCircle" app/onboarding.tsx shows width: 220 or width:220
  - grep -n "fontSize: 30" app/onboarding.tsx returns a match in title style
  </acceptance_criteria>
  <verify>
    <automated>
    cd /c/Users/User/Desktop/Projeto/app_defasaCivil/app_defesa_civil_expo && grep -n "scrollEnabled={true}" app/onboarding.tsx && grep -n "onMomentumScrollEnd" app/onboarding.tsx && grep -n "from '../../components/ui'" app/onboarding.tsx
    </automated>
  </verify>
  <done>
  app/onboarding.tsx has swipeable FlatList with dot sync, enlarged icons (220x220), improved typography (fontSize 30 / weight 800), and Button components from the design system replacing both TouchableOpacity buttons.
  </done>
</task>

</tasks>

<verification>
grep -n "scrollEnabled={true}" app/onboarding.tsx
grep -n "onMomentumScrollEnd" app/onboarding.tsx
grep -n "Button" app/onboarding.tsx
</verification>

<success_criteria>
- scrollEnabled={true} present on FlatList
- onMomentumScrollEnd handler syncs dot indicator
- Both Pular and Proximo/Comecar use Button from design system
- iconCircle/iconInner enlarged to 220/160
- Title typography: fontSize 30, fontWeight 800, letterSpacing -0.8
</success_criteria>

<output>
After completion, create `.planning/phases/03-ui-auth-agente/03-01-SUMMARY.md`
</output>
