---
phase: 03-ui-auth-agente
plan: 06
type: execute
wave: 2
depends_on: []
files_modified:
  - app/(panel)/dashboard.tsx
autonomous: true
requirements:
  - PERF-01
  - UX-redesign
must_haves:
  truths:
    - "useMemo with empty dependency array computes diaSemana and dataFormatada on mount only"
    - "KPI cards use Card component from design system"
    - "ErrorState is shown when fetchMetrics fails (metricsError state exists)"
    - "KPIs have gap: 16 and internal padding: 20"
  artifacts:
    - path: "app/(panel)/dashboard.tsx"
      provides: "Dashboard with memoized date/time, Card-based KPIs, and ErrorState on metrics failure"
  key_links:
    - from: "app/(panel)/dashboard.tsx"
      to: "useMemo"
      via: "React.useMemo with empty dep array for date/time computation"
      pattern: "useMemo"
    - from: "app/(panel)/dashboard.tsx"
      to: "components/ui/ErrorState"
      via: "rendered when metricsError !== null"
      pattern: "ErrorState"
---

<objective>
Apply PERF-01 performance fix (useMemo for date/time) and visual improvements to the dashboard: Card-based KPIs, ErrorState on failure, and better spacing.

Purpose: Dashboard is the home screen — memoized date/time prevents unnecessary recalculations; ErrorState prevents silent failures when offline.
Output: Modified app/(panel)/dashboard.tsx with useMemo, Card components, ErrorState, and breathing room in KPI section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ui-auth-agente/03-RESEARCH.md

<interfaces>
Card: children, style (optional)
Button: variant="primary"|"secondary"|"ghost"|"danger", disabled, loading, onPress, children
ErrorState: message, onRetry
<!-- useTheme() → { theme: typeof Colors.light } -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard — useMemo for date/time + Card KPIs + ErrorState</name>
  <files>app/(panel)/dashboard.tsx</files>
  <read_first>
    - app/(panel)/dashboard.tsx (read BEFORE editing — understand current DIAS_SEMANA/MESES arrays, fetchMetrics function, KPI card rendering, existing error handling)
    - components/ui/index.ts (verify Card, ErrorState exports)
  </read_first>
  <action>
  Read the full current file first. Then apply all changes below.

  FUNCTIONAL CHANGE — PERF-01 (required):
  Find the lines that calculate hoje, diaSemana, dataFormatada (currently inline in the component body or in an effect). Remove those 3 lines.
  Add this useMemo block at the top of the component function (after state declarations):
  ```typescript
  import { useMemo } from 'react';
  const { diaSemana, dataFormatada } = useMemo(() => {
    const hoje = new Date();
    const diaSemana = DIAS_SEMANA[hoje.getDay()];
    const dataFormatada = `${hoje.getDate()} de ${MESES[hoje.getMonth()]}`;
    return { diaSemana, dataFormatada };
  }, []); // dependência vazia: recalcular só na montagem
  ```
  DIAS_SEMANA and MESES arrays should remain as-is — they are already defined in the file.

  FUNCTIONAL CHANGE — ERRORSTATE ON METRICS FAILURE:
  Add state: `const [metricsError, setMetricsError] = useState<string | null>(null)`
  In the fetchMetrics function, in the catch block, add: `setMetricsError('Não foi possível carregar as métricas')`
  At the start of fetchMetrics (or onRefresh), add: `setMetricsError(null)` to clear on retry.

  VISUAL CHANGE — KPI CARDS:
  Import Card and ErrorState:
  ```tsx
  import { Card, ErrorState } from '../../components/ui';
  ```
  Replace each kpiCard View with Card component. Keep all internal content (icon, value, label) intact.
  In the KPI section, wrap in a container with gap: 16.
  Inside each Card, add padding: 20 to the Card's internal View.

  When metricsError !== null, render ErrorState instead of KPI cards:
  ```tsx
  {metricsError !== null ? (
    <ErrorState message={metricsError} onRetry={onRefresh} />
  ) : (
    {/* KPI cards */}
  )}
  ```

  VISUAL CHANGE — SHORTCUT CARDS:
  Replace gridCard View wrappers with Card component for consistency. Keep internal content.

  IMPORTANT: Do not remove or alter the onRefresh function, the RefreshControl, or any navigation logic.
  </action>
  <acceptance_criteria>
  - grep -n "useMemo" app/(panel)/dashboard.tsx returns a match with empty dep array
  - grep -n "metricsError" app/(panel)/dashboard.tsx returns matches for state and usage
  - grep -n "ErrorState" app/(panel)/dashboard.tsx returns matches for import and usage
  - grep -n "Card" app/(panel)/dashboard.tsx returns matches for import and usage
  </acceptance_criteria>
  <verify>
    <automated>
    cd /c/Users/User/Desktop/Projeto/app_defasaCivil/app_defesa_civil_expo && grep -n "useMemo" "app/(panel)/dashboard.tsx" && grep -n "metricsError" "app/(panel)/dashboard.tsx" && grep -n "ErrorState" "app/(panel)/dashboard.tsx"
    </automated>
  </verify>
  <done>
  app/(panel)/dashboard.tsx has useMemo with empty deps for date/time, KPI cards wrapped in Card component with gap: 16 spacing, and ErrorState shown when fetchMetrics fails.
  </done>
</task>

</tasks>

<verification>
grep -n "useMemo" "app/(panel)/dashboard.tsx"
grep -n "metricsError" "app/(panel)/dashboard.tsx"
grep -n "ErrorState" "app/(panel)/dashboard.tsx"
grep -n "Card" "app/(panel)/dashboard.tsx"
</verification>

<success_criteria>
- useMemo(() => ..., []) present — empty dependency array for date/time
- metricsError state added, set in catch, cleared on retry
- KPI section wrapped in ErrorState conditional
- Card component used for KPI and shortcut cards
</success_criteria>

<output>
After completion, create `.planning/phases/03-ui-auth-agente/03-06-SUMMARY.md`
</output>
