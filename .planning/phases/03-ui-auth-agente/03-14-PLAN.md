---
phase: 03-ui-auth-agente
plan: 14
type: execute
wave: 3
depends_on: [03-08]
files_modified:
  - app/(panel)/inspecoes/laudo.tsx
autonomous: true
requirements:
  - UX-redesign
must_haves:
  truths:
    - "Header uses surfaceHighlight background with border and paddingTop 60"
    - "Back button uses 44x44 borderRadius 12 iconBackground/border style"
    - "PDF generation button uses Button variant=primary from design system"
    - "LoadingState replaces ActivityIndicator during PDF generation"
    - "No changes to PDF generation logic — only visual wrapping"
  artifacts:
    - path: "app/(panel)/inspecoes/laudo.tsx"
      provides: "Laudo screen visually consistent with other inspection flow screens"
  key_links:
    - from: "app/(panel)/inspecoes/laudo.tsx"
      to: "components/ui/Button"
      via: "import and usage for PDF generation action"
      pattern: "Button"
    - from: "app/(panel)/inspecoes/laudo.tsx"
      to: "components/ui/LoadingState"
      via: "shown during PDF generation loading"
      pattern: "LoadingState"
---

<objective>
Apply visual consistency to laudo.tsx: matching header, back button, Button component for actions, and LoadingState. Zero changes to PDF generation logic — that refactoring is scoped to Phase 5.

Purpose: laudo.tsx is the last screen in the inspection flow — visual consistency with the preceding screens completes the flow's unified feel.
Output: Modified app/(panel)/inspecoes/laudo.tsx with consistent header, back button, Button actions, and LoadingState.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ui-auth-agente/03-RESEARCH.md

<interfaces>
Button: variant="primary"|"secondary"|"ghost"|"danger", disabled, loading, onPress, children
LoadingState: (zero props)
<!-- theme.surfaceHighlight, theme.border, theme.iconBackground, theme.text, theme.muted -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Laudo — consistent header + back button + Button actions + LoadingState</name>
  <files>app/(panel)/inspecoes/laudo.tsx</files>
  <read_first>
    - app/(panel)/inspecoes/laudo.tsx (read BEFORE editing — understand current header, action buttons, PDF generation state, ActivityIndicator usage)
    - components/ui/index.ts (verify Button, LoadingState exports)
  </read_first>
  <action>
  Read the full current file first. Then apply all changes below. Do NOT modify any PDF generation, HTML building, or sharing logic.

  IMPORTS:
  ```tsx
  import { Button, LoadingState } from '../../components/ui';
  ```

  VISUAL CHANGE — HEADER:
  Update header container style to match other inspection flow screens:
  ```
  backgroundColor: theme.surfaceHighlight
  borderBottomColor: theme.border
  borderBottomWidth: 1
  paddingTop: 60
  paddingBottom: 16
  paddingHorizontal: 20
  ```

  VISUAL CHANGE — BACK BUTTON:
  Update backButton style:
  ```
  width: 44, height: 44, borderRadius: 12, borderWidth: 1,
  backgroundColor: theme.iconBackground, borderColor: theme.border,
  alignItems: 'center', justifyContent: 'center'
  ```

  VISUAL CHANGE — LOADINGSTATE:
  Find where ActivityIndicator is used during PDF loading/generation. Replace with:
  ```tsx
  {gerandoPdf && <LoadingState />}
  ```
  (Use the actual boolean state variable name from the file.)

  VISUAL CHANGE — ACTION BUTTONS:
  Replace the PDF generation button (gerar PDF, baixar, compartilhar, etc.) with Button components:
  ```tsx
  <Button
    variant="primary"
    loading={gerandoPdf}
    onPress={handleGerarPdf}
    disabled={gerandoPdf}
  >
    Gerar Laudo PDF
  </Button>
  ```
  If there is a share/download button, use Button variant="secondary".
  Use the actual handler names and loading state variable from the file.

  CRITICAL: Do NOT touch any logic inside handleGerarPdf, PDF HTML building, print sharing, or WebView usage. Only replace the outer UI elements.
  </action>
  <acceptance_criteria>
  - grep -n "surfaceHighlight" app/(panel)/inspecoes/laudo.tsx returns a match in header style
  - grep -n "Button" app/(panel)/inspecoes/laudo.tsx returns matches for import and usage
  - grep -n "LoadingState" app/(panel)/inspecoes/laudo.tsx returns a match
  - grep -n "iconBackground" app/(panel)/inspecoes/laudo.tsx returns a match in backButton style
  </acceptance_criteria>
  <verify>
    <automated>
    cd /c/Users/User/Desktop/Projeto/app_defasaCivil/app_defesa_civil_expo && grep -n "surfaceHighlight" "app/(panel)/inspecoes/laudo.tsx" && grep -n "Button" "app/(panel)/inspecoes/laudo.tsx" && grep -n "LoadingState" "app/(panel)/inspecoes/laudo.tsx"
    </automated>
  </verify>
  <done>
  app/(panel)/inspecoes/laudo.tsx has the same header style as risco/resultado/foto screens (surfaceHighlight, border, paddingTop 60), back button with iconBackground/border, Button variant="primary" for PDF generation, and LoadingState during generation. PDF generation logic is untouched.
  </done>
</task>

</tasks>

<verification>
grep -n "surfaceHighlight" "app/(panel)/inspecoes/laudo.tsx"
grep -n "Button" "app/(panel)/inspecoes/laudo.tsx"
grep -n "LoadingState" "app/(panel)/inspecoes/laudo.tsx"
grep -n "iconBackground" "app/(panel)/inspecoes/laudo.tsx"
</verification>

<success_criteria>
- Header: backgroundColor surfaceHighlight, borderBottomColor border, paddingTop 60, paddingBottom 16, paddingHorizontal 20
- Back button: 44x44, borderRadius 12, iconBackground, border token
- Button variant="primary" with loading prop for PDF action
- LoadingState replaces ActivityIndicator during generation
- Zero changes to PDF generation, HTML, or sharing logic
</success_criteria>

<output>
After completion, create `.planning/phases/03-ui-auth-agente/03-14-SUMMARY.md`
</output>
