---
phase: 03-ui-auth-agente
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(auth)/forgot-password.tsx
autonomous: true
requirements:
  - UX-redesign
must_haves:
  truths:
    - "Layout uses SafeAreaView + KeyboardAvoidingView + ScrollView pattern"
    - "Submit button uses Button variant=primary from design system"
    - "Success state is shown after email is sent with mail icon and confirmation message"
    - "Error displays with rgba(239,68,68,0.08) background banner (same as login and register)"
    - "Back button uses iconBackground and border tokens"
  artifacts:
    - path: "app/(auth)/forgot-password.tsx"
      provides: "Forgot password screen visually consistent with login.tsx and register.tsx"
  key_links:
    - from: "app/(auth)/forgot-password.tsx"
      to: "components/ui/Button"
      via: "import and usage for submit button"
      pattern: "Button"
---

<objective>
Bring forgot-password.tsx into visual consistency with login.tsx and register.tsx by applying the same layout pattern, error banner style, design system Button, and a post-submission success state.

Purpose: Consistency across auth screens reduces cognitive friction and makes the app feel polished.
Output: Modified app/(auth)/forgot-password.tsx matching auth screen visual conventions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ui-auth-agente/03-RESEARCH.md

<interfaces>
Button: variant="primary"|"secondary"|"ghost"|"danger", disabled, loading, onPress, children
<!-- Theme tokens: theme.iconBackground, theme.border, theme.primary, theme.textSecondary, theme.success -->
<!-- Feather icons: alert-circle, mail, check-circle, arrow-left -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Forgot-password — layout pattern, error banner, success state, design system button</name>
  <files>app/(auth)/forgot-password.tsx</files>
  <read_first>
    - app/(auth)/forgot-password.tsx (read BEFORE editing — understand current layout, submit handler, state variables)
    - app/(auth)/login.tsx (reference for exact pattern to match if already modified in plan 03-03)
    - components/ui/index.ts (verify Button export)
  </read_first>
  <action>
  Read the full current file first. Then apply all changes below without altering the existing Supabase password reset logic.

  LAYOUT (verify and apply if not already correct):
  Ensure outer structure is:
  ```tsx
  <SafeAreaView style={{ flex: 1, backgroundColor: theme.background }}>
    <KeyboardAvoidingView behavior={Platform.OS === 'ios' ? 'padding' : 'height'} style={{ flex: 1 }}>
      <ScrollView contentContainerStyle={{ flexGrow: 1, padding: 20 }} keyboardShouldPersistTaps="handled">
        {/* content */}
      </ScrollView>
    </KeyboardAvoidingView>
  </SafeAreaView>
  ```

  BACK BUTTON in header area:
  ```tsx
  <TouchableOpacity
    onPress={() => router.back()}
    style={{
      width: 44, height: 44, borderRadius: 12, borderWidth: 1,
      backgroundColor: theme.iconBackground, borderColor: theme.border,
      alignItems: 'center', justifyContent: 'center',
    }}
  >
    <Feather name="arrow-left" size={20} color={theme.text} />
  </TouchableOpacity>
  ```

  INPUT FIELD style (verify/apply):
  inputContainer: borderRadius: 16, height: 60, paddingHorizontal: 16

  ERROR BANNER (same as login.tsx):
  Replace any plain `<Text style={errorText}>` with:
  ```tsx
  {error !== null && (
    <View style={{
      flexDirection: 'row', alignItems: 'center',
      backgroundColor: 'rgba(239,68,68,0.08)', borderWidth: 1,
      borderColor: 'rgba(239,68,68,0.2)', borderRadius: 12,
      padding: 12, gap: 8, marginBottom: 12,
    }}>
      <Feather name="alert-circle" size={16} color="#EF4444" />
      <Text style={{ color: '#EF4444', fontSize: 14, flex: 1 }}>{error}</Text>
    </View>
  )}
  ```

  SUBMIT BUTTON:
  Replace existing submit button with:
  ```tsx
  import { Button } from '../../components/ui';
  <Button variant="primary" loading={loading} onPress={handleSubmit} disabled={loading}>
    Enviar Email de Recuperação
  </Button>
  ```
  (Use the actual handler name from the file — do not rename it.)

  SUCCESS STATE:
  Add a boolean state: `const [enviado, setEnviado] = useState(false)`
  Set `setEnviado(true)` after successful Supabase reset email call.
  When enviado === true, render instead of the form:
  ```tsx
  <View style={{ alignItems: 'center', padding: 24 }}>
    <View style={{ width: 80, height: 80, borderRadius: 40, backgroundColor: theme.successLight || '#F0FDF4', alignItems: 'center', justifyContent: 'center', marginBottom: 20 }}>
      <Feather name="mail" size={36} color={theme.success} />
    </View>
    <Text style={{ fontSize: 20, fontWeight: '700', textAlign: 'center' }}>Email enviado!</Text>
    <Text style={{ color: theme.textSecondary, textAlign: 'center', marginTop: 8, lineHeight: 22 }}>
      Verifique sua caixa de entrada e siga as instruções para redefinir sua senha.
    </Text>
    <Button variant="secondary" onPress={() => router.back()} style={{ marginTop: 24 }}>
      Voltar ao Login
    </Button>
  </View>
  ```

  FUNCTIONAL CHANGES: None — keep all Supabase logic intact.
  </action>
  <acceptance_criteria>
  - grep -n "Button" app/(auth)/forgot-password.tsx returns matches for import and usage
  - grep -n "rgba(239,68,68,0.08)" app/(auth)/forgot-password.tsx returns a match
  - grep -n "enviado" app/(auth)/forgot-password.tsx returns a match (success state)
  - grep -n "SafeAreaView" app/(auth)/forgot-password.tsx returns a match
  - grep -n "KeyboardAvoidingView" app/(auth)/forgot-password.tsx returns a match
  </acceptance_criteria>
  <verify>
    <automated>
    cd /c/Users/User/Desktop/Projeto/app_defasaCivil/app_defesa_civil_expo && grep -n "Button" "app/(auth)/forgot-password.tsx" && grep -n "rgba(239,68,68" "app/(auth)/forgot-password.tsx" && grep -n "enviado" "app/(auth)/forgot-password.tsx"
    </automated>
  </verify>
  <done>
  app/(auth)/forgot-password.tsx uses SafeAreaView + KeyboardAvoidingView + ScrollView layout, error banner matches login/register, submit uses Button variant="primary", and a post-submission success state with mail icon replaces the form.
  </done>
</task>

</tasks>

<verification>
grep -n "Button" "app/(auth)/forgot-password.tsx"
grep -n "rgba(239,68,68,0.08)" "app/(auth)/forgot-password.tsx"
grep -n "enviado" "app/(auth)/forgot-password.tsx"
grep -n "KeyboardAvoidingView" "app/(auth)/forgot-password.tsx"
</verification>

<success_criteria>
- SafeAreaView + KeyboardAvoidingView + ScrollView outer structure
- Error banner with rgba(239,68,68,0.08) background (matches login and register)
- Button variant="primary" with loading for submit
- Success state shown after email sent: mail icon + confirmation text + back button
- Back button with iconBackground/border tokens
</success_criteria>

<output>
After completion, create `.planning/phases/03-ui-auth-agente/03-05-SUMMARY.md`
</output>
