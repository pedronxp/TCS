---
phase: 03-ui-auth-agente
plan: 08
type: execute
wave: 2
depends_on: []
files_modified:
  - app/(panel)/inspecoes/index.tsx
autonomous: true
requirements:
  - UX-redesign
must_haves:
  truths:
    - "EmptyState component replaces the inline ListEmptyComponent"
    - "LoadingState component replaces the standalone ActivityIndicator"
    - "ErrorState component is shown when fetchVistorias fails"
    - "fetchError state is cleared before each new fetch attempt"
    - "Card component wraps inspection list items"
  artifacts:
    - path: "app/(panel)/inspecoes/index.tsx"
      provides: "Inspections list with EmptyState, LoadingState, ErrorState, and Card wrappers"
  key_links:
    - from: "app/(panel)/inspecoes/index.tsx"
      to: "components/ui/EmptyState"
      via: "used as ListEmptyComponent in FlatList"
      pattern: "EmptyState"
    - from: "app/(panel)/inspecoes/index.tsx"
      to: "components/ui/ErrorState"
      via: "rendered when fetchError !== null"
      pattern: "ErrorState"
---

<objective>
Replace the inline empty/loading states in the inspections list screen with proper design system components (EmptyState, LoadingState, ErrorState, Card).

Purpose: Consistent empty/error/loading states across all list screens makes the app feel complete and prevents silent failures.
Output: Modified app/(panel)/inspecoes/index.tsx with design system state components and Card-wrapped list items.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ui-auth-agente/03-RESEARCH.md

<interfaces>
EmptyState: icon (Feather name), title, description, actionLabel, onAction
LoadingState: (zero props)
ErrorState: message, onRetry
Card: children, style (optional)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inspecoes index — EmptyState + LoadingState + ErrorState + Card wrappers</name>
  <files>app/(panel)/inspecoes/index.tsx</files>
  <read_first>
    - app/(panel)/inspecoes/index.tsx (read BEFORE editing — understand FlatList structure, loading state, existing empty component, fetchVistorias error handling)
    - components/ui/index.ts (verify all 4 component exports)
  </read_first>
  <action>
  Read the full current file first. Then apply all changes below.

  IMPORTS — add at top:
  ```tsx
  import { Card, EmptyState, LoadingState, ErrorState } from '../../components/ui';
  ```

  FUNCTIONAL CHANGE — ERRORSTATE STATE:
  Add state: `const [fetchError, setFetchError] = useState<string | null>(null)`
  In fetchVistorias (or equivalent fetch function), in the catch block, add:
  ```typescript
  setFetchError('Erro ao carregar vistorias. Toque para tentar novamente.');
  ```
  At the START of fetchVistorias, before the try, add:
  ```typescript
  setFetchError(null);
  ```

  VISUAL CHANGE — REPLACE LISTEMPYCOMPONENT:
  Replace whatever is currently in ListEmptyComponent (inline View or Text) with:
  ```tsx
  ListEmptyComponent={
    !loading ? (
      <EmptyState
        icon="clipboard"
        title="Nenhuma vistoria encontrada"
        description="Toque no botão + para registrar a primeira vistoria."
        actionLabel="Nova Vistoria"
        onAction={() => router.push('/(panel)/inspecoes/dados-iniciais')}
      />
    ) : null
  }
  ```

  VISUAL CHANGE — REPLACE ACTIVITYINDICATOR:
  Replace the standalone ActivityIndicator loading indicator with:
  ```tsx
  {loading && <LoadingState />}
  ```
  (The location depends on the current file — it may be a conditional render before the FlatList or a separate screen block.)

  VISUAL CHANGE — ERRORSTATE CONDITIONAL:
  Before the FlatList render (or in its place when there's an error), add:
  ```tsx
  {fetchError !== null && (
    <ErrorState
      message={fetchError}
      onRetry={() => fetchVistorias(profile!)}
    />
  )}
  ```
  When fetchError is not null, either hide the FlatList or show ErrorState above it — choose the approach that fits the current file structure.

  VISUAL CHANGE — CARD WRAPPERS:
  In the FlatList renderItem or InspecaoCard component (whichever renders the list item), wrap the outer View with Card:
  ```tsx
  <Card style={{ marginBottom: 12 }}>
    {/* existing card content unchanged */}
  </Card>
  ```
  Keep all internal content (status badges, date, address, etc.) exactly as-is.
  </action>
  <acceptance_criteria>
  - grep -n "EmptyState" app/(panel)/inspecoes/index.tsx returns a match
  - grep -n "LoadingState" app/(panel)/inspecoes/index.tsx returns a match
  - grep -n "ErrorState" app/(panel)/inspecoes/index.tsx returns a match
  - grep -n "fetchError" app/(panel)/inspecoes/index.tsx returns a match for state and usage
  - grep -n "Card" app/(panel)/inspecoes/index.tsx returns a match
  </acceptance_criteria>
  <verify>
    <automated>
    cd /c/Users/User/Desktop/Projeto/app_defasaCivil/app_defesa_civil_expo && grep -n "EmptyState" "app/(panel)/inspecoes/index.tsx" && grep -n "ErrorState" "app/(panel)/inspecoes/index.tsx" && grep -n "LoadingState" "app/(panel)/inspecoes/index.tsx"
    </automated>
  </verify>
  <done>
  app/(panel)/inspecoes/index.tsx has EmptyState in FlatList ListEmptyComponent, LoadingState replacing ActivityIndicator, ErrorState with retry shown on fetch failure, and Card wrapping list items.
  </done>
</task>

</tasks>

<verification>
grep -n "EmptyState" "app/(panel)/inspecoes/index.tsx"
grep -n "LoadingState" "app/(panel)/inspecoes/index.tsx"
grep -n "ErrorState" "app/(panel)/inspecoes/index.tsx"
grep -n "fetchError" "app/(panel)/inspecoes/index.tsx"
</verification>

<success_criteria>
- EmptyState with clipboard icon, title, description, and Nova Vistoria action
- LoadingState replaces ActivityIndicator
- fetchError state cleared before each fetch
- ErrorState with onRetry when fetch fails
- Card wrapping inspection list items
</success_criteria>

<output>
After completion, create `.planning/phases/03-ui-auth-agente/03-08-SUMMARY.md`
</output>
