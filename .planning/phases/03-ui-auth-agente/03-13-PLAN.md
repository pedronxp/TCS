---
phase: 03-ui-auth-agente
plan: 13
type: execute
wave: 3
depends_on: [03-08]
files_modified:
  - app/(panel)/inspecoes/risco.tsx
  - app/(panel)/inspecoes/resultado.tsx
  - app/(panel)/inspecoes/foto.tsx
autonomous: true
requirements:
  - UX-redesign
must_haves:
  truths:
    - "All 3 screens share the same header style: surfaceHighlight background, border, paddingTop 60"
    - "All 3 backButtons use: width 44, height 44, borderRadius 12, borderWidth 1, iconBackground, border"
    - "resultado.tsx has 3 action buttons: Button primary (Baixar PDF), secondary (Imprimir), secondary (Compartilhar)"
    - "resultado.tsx replaces ActivityIndicator with LoadingState"
    - "foto.tsx has EmptyState with camera icon when no photos"
    - "risco.tsx uses Card for risk card and Badge for risk level display"
  artifacts:
    - path: "app/(panel)/inspecoes/risco.tsx"
      provides: "Risk screen with consistent header and Card/Badge for risk display"
    - path: "app/(panel)/inspecoes/resultado.tsx"
      provides: "Result screen with consistent header, 3 Button actions, LoadingState"
    - path: "app/(panel)/inspecoes/foto.tsx"
      provides: "Photo screen with consistent header and EmptyState when no photos"
  key_links:
    - from: "app/(panel)/inspecoes/resultado.tsx"
      to: "components/ui/Button"
      via: "3 action buttons: primary + secondary + secondary"
      pattern: "Button"
    - from: "app/(panel)/inspecoes/foto.tsx"
      to: "components/ui/EmptyState"
      via: "shown when photos list is empty"
      pattern: "EmptyState"
---

<objective>
Apply consistent header style and design system components to risco.tsx, resultado.tsx, and foto.tsx. These 3 screens are the final steps of the inspection flow and must feel visually uniform.

Purpose: Visual consistency in the inspection flow reduces cognitive load. resultado.tsx with 3 distinct button variants makes actions clearly distinguishable.
Output: Modified risco.tsx, resultado.tsx, and foto.tsx with uniform headers, design system components, and proper async states.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ui-auth-agente/03-RESEARCH.md

<interfaces>
Card: children, style (optional)
Badge: variant (R1|R2|R3|R4|success|warning|error), children, style
Button: variant="primary"|"secondary"|"ghost"|"danger", disabled, loading, onPress, children
EmptyState: icon (Feather name), title, description, actionLabel, onAction
LoadingState: (zero props)
<!-- theme.surfaceHighlight, theme.border, theme.iconBackground, theme.muted -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: risco.tsx — consistent header + Card + Badge for risk level</name>
  <files>app/(panel)/inspecoes/risco.tsx</files>
  <read_first>
    - app/(panel)/inspecoes/risco.tsx (read BEFORE editing — understand header structure, risk card rendering, back button style)
    - components/ui/index.ts (verify Card, Badge exports)
  </read_first>
  <action>
  Read the full current file first. Then apply all changes.

  IMPORTS:
  ```tsx
  import { Card, Badge, Button } from '../../components/ui';
  ```

  VISUAL CHANGE — HEADER:
  Update header container style to:
  ```
  backgroundColor: theme.surfaceHighlight
  borderBottomColor: theme.border
  borderBottomWidth: 1
  paddingTop: 60
  paddingBottom: 16
  paddingHorizontal: 20
  ```

  VISUAL CHANGE — BACK BUTTON:
  Update backButton style to:
  ```
  width: 44, height: 44, borderRadius: 12, borderWidth: 1,
  backgroundColor: theme.iconBackground, borderColor: theme.border,
  alignItems: 'center', justifyContent: 'center'
  ```

  VISUAL CHANGE — RISK CARD:
  Replace existing risk card View with Card component. Keep internal content.
  Replace risk level display (text or inline badge) with:
  ```tsx
  <Badge variant={riscoLevel as any}>
    {riscoLabel}
  </Badge>
  ```
  Where riscoLevel is r1/r2/r3/r4 from the current state/prop, and riscoLabel is the display text.

  VISUAL CHANGE — ACTION BUTTONS:
  Replace any action buttons with Button component from design system.

  Do NOT change any risk calculation logic or navigation.
  </action>
  <acceptance_criteria>
  - grep -n "surfaceHighlight" app/(panel)/inspecoes/risco.tsx returns a match in header
  - grep -n "Badge" app/(panel)/inspecoes/risco.tsx returns matches
  - grep -n "Card" app/(panel)/inspecoes/risco.tsx returns matches
  </acceptance_criteria>
  <verify>
    <automated>
    cd /c/Users/User/Desktop/Projeto/app_defasaCivil/app_defesa_civil_expo && grep -n "surfaceHighlight" "app/(panel)/inspecoes/risco.tsx" && grep -n "Badge" "app/(panel)/inspecoes/risco.tsx"
    </automated>
  </verify>
  <done>
  risco.tsx has consistent header (surfaceHighlight, border, paddingTop 60), backButton with iconBackground/border, Card-wrapped risk display, and Badge for risk level.
  </done>
</task>

<task type="auto">
  <name>Task 2: resultado.tsx and foto.tsx — consistent headers, Button actions, LoadingState, EmptyState</name>
  <files>app/(panel)/inspecoes/resultado.tsx, app/(panel)/inspecoes/foto.tsx</files>
  <read_first>
    - app/(panel)/inspecoes/resultado.tsx (read BEFORE editing — understand current action buttons, loading state, PDF generation calls)
    - app/(panel)/inspecoes/foto.tsx (read BEFORE editing — understand photo list state, loading, existing empty check)
    - components/ui/index.ts (verify Button, LoadingState, EmptyState exports)
  </read_first>
  <action>
  Read both files first. Apply changes to each.

  === resultado.tsx ===

  IMPORTS:
  ```tsx
  import { Card, Badge, Button, LoadingState } from '../../components/ui';
  ```

  VISUAL CHANGE — HEADER (same pattern):
  ```
  backgroundColor: theme.surfaceHighlight, borderBottomColor: theme.border,
  borderBottomWidth: 1, paddingTop: 60, paddingBottom: 16, paddingHorizontal: 20
  ```

  VISUAL CHANGE — BACK BUTTON:
  width: 44, height: 44, borderRadius: 12, borderWidth: 1, backgroundColor: theme.iconBackground, borderColor: theme.border

  VISUAL CHANGE — RISK CARD:
  Wrap risk level display in Card. Use Badge for the risk level with appropriate variant.

  VISUAL CHANGE — LOADING:
  Replace ActivityIndicator loading state with `<LoadingState />`

  VISUAL CHANGE — ACTION BUTTONS (important — 3 distinct variants):
  Replace the 3 action buttons with:
  ```tsx
  <Button variant="primary" onPress={handleBaixarPdf} loading={gerandoPdf}>
    Baixar PDF
  </Button>
  <Button variant="secondary" onPress={handleImprimir}>
    Imprimir
  </Button>
  <Button variant="secondary" onPress={handleCompartilhar}>
    Compartilhar
  </Button>
  ```
  Use the actual handler function names from the file. Do NOT change the PDF generation logic.

  === foto.tsx ===

  IMPORTS:
  ```tsx
  import { EmptyState, LoadingState, Button } from '../../components/ui';
  ```

  VISUAL CHANGE — HEADER (same pattern as resultado.tsx):
  Same surfaceHighlight/border/paddingTop 60 style.

  VISUAL CHANGE — BACK BUTTON:
  Same 44x44 borderRadius 12 iconBackground style.

  VISUAL CHANGE — LOADING:
  Replace ActivityIndicator with `<LoadingState />` while loading photos.

  VISUAL CHANGE — EMPTY STATE:
  Replace any inline empty message with:
  ```tsx
  {fotos.length === 0 && !loading && (
    <EmptyState
      icon="camera"
      title="Nenhuma foto registrada"
      description="Tire uma foto para documentar a vistoria."
      actionLabel="Tirar Foto"
      onAction={handleTirarFoto}
    />
  )}
  ```
  Use the actual state variable name for photos list and the actual handler for taking photos.

  VISUAL CHANGE — TAKE PHOTO BUTTON:
  Replace existing take-photo button with Button variant="primary".
  </action>
  <acceptance_criteria>
  - grep -n "surfaceHighlight" app/(panel)/inspecoes/resultado.tsx returns a match
  - grep -n "LoadingState" app/(panel)/inspecoes/resultado.tsx returns a match
  - grep -n "variant=\"primary\"" app/(panel)/inspecoes/resultado.tsx returns a match (Baixar PDF button)
  - grep -n "variant=\"secondary\"" app/(panel)/inspecoes/resultado.tsx returns 2 matches (Imprimir + Compartilhar)
  - grep -n "EmptyState" app/(panel)/inspecoes/foto.tsx returns a match
  - grep -n "camera" app/(panel)/inspecoes/foto.tsx returns a match (EmptyState icon)
  - grep -n "surfaceHighlight" app/(panel)/inspecoes/foto.tsx returns a match
  </acceptance_criteria>
  <verify>
    <automated>
    cd /c/Users/User/Desktop/Projeto/app_defasaCivil/app_defesa_civil_expo && grep -n "surfaceHighlight" "app/(panel)/inspecoes/resultado.tsx" && grep -n "LoadingState" "app/(panel)/inspecoes/resultado.tsx" && grep -n "EmptyState" "app/(panel)/inspecoes/foto.tsx"
    </automated>
  </verify>
  <done>
  resultado.tsx has consistent header, LoadingState, Card+Badge risk display, and 3 action Button components (primary+secondary+secondary). foto.tsx has consistent header, LoadingState while loading, and EmptyState with camera icon when no photos.
  </done>
</task>

</tasks>

<verification>
grep -n "surfaceHighlight" "app/(panel)/inspecoes/risco.tsx"
grep -n "surfaceHighlight" "app/(panel)/inspecoes/resultado.tsx"
grep -n "surfaceHighlight" "app/(panel)/inspecoes/foto.tsx"
grep -n "EmptyState" "app/(panel)/inspecoes/foto.tsx"
grep -n "LoadingState" "app/(panel)/inspecoes/resultado.tsx"
</verification>

<success_criteria>
- All 3 headers: backgroundColor surfaceHighlight, borderBottomColor border, paddingTop 60, paddingBottom 16, paddingHorizontal 20
- All 3 backButtons: 44x44, borderRadius 12, iconBackground, border
- resultado.tsx: 3 Button actions (primary: Baixar PDF, secondary: Imprimir, secondary: Compartilhar)
- resultado.tsx: LoadingState instead of ActivityIndicator
- foto.tsx: EmptyState icon="camera" when photos list empty
- risco.tsx: Card + Badge for risk level display
</success_criteria>

<output>
After completion, create `.planning/phases/03-ui-auth-agente/03-13-SUMMARY.md`
</output>
