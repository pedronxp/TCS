---
phase: 11-mapa-nativo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(panel)/mapas.tsx
  - package.json
autonomous: false
requirements:
  - MAPA-01
  - MAPA-02

must_haves:
  truths:
    - "Agente abre a tela de mapa e o MapView renderiza sem tela branca no Android e iOS"
    - "Marcadores de vistoria aparecem no mapa com as cores de risco corretas"
    - "Filtros por risco e período continuam funcionando após a troca"
    - "Heatmap continua disponível no Android (guard Platform.OS === 'android' mantido)"
    - "Popup de marcador, FABs, modal de estilo e fitToMarkers continuam funcionando"
  artifacts:
    - path: "app/(panel)/mapas.tsx"
      provides: "Tela de mapa usando MapView padrão do react-native-maps"
      contains: "import MapView"
    - path: "package.json"
      provides: "Dependências limpas — react-native-map-clustering e leaflet removidos"
  key_links:
    - from: "app/(panel)/mapas.tsx"
      to: "react-native-maps"
      via: "import MapView from 'react-native-maps'"
      pattern: "import MapView"
    - from: "MapView"
      to: "PROVIDER_GOOGLE"
      via: "provider prop no Android"
      pattern: "PROVIDER_GOOGLE"
---

<objective>
Corrigir tela branca do mapa substituindo ClusteredMapView (react-native-map-clustering) pelo MapView padrão do react-native-maps, e remover dependências obsoletas do package.json.

Purpose: react-native-map-clustering 4.0.0 tem crashes conhecidos com Expo SDK 54 + React Native 0.81 (New Architecture). A substituição é cirúrgica — mesmo provider, mesmos filhos, mesmas props, apenas removendo o wrapper problemático.

Output: mapas.tsx funcional sem ClusteredMapView; package.json sem react-native-map-clustering, leaflet, leaflet.heat e leaflet.markercluster.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Estado atual de app/(panel)/mapas.tsx — extraído para referência do executor -->

Imports relevantes (linhas 6-7):
```typescript
import MapView, { Marker, Heatmap, PROVIDER_GOOGLE, PROVIDER_DEFAULT, MapType } from 'react-native-maps';
import ClusteredMapView from 'react-native-map-clustering';  // <-- REMOVER esta linha
```

Ref do mapa (linha 83):
```typescript
const mapRef = useRef<MapView>(null);
// Já tipado como MapView — sem alteração necessária
```

JSX atual (linhas 257-293):
```tsx
<ClusteredMapView
  ref={mapRef}
  style={StyleSheet.absoluteFillObject}
  provider={Platform.OS === 'android' ? PROVIDER_GOOGLE : PROVIDER_DEFAULT}
  mapType={currentStyleConfig.mapType}
  customMapStyle={mapStyle === 'escuro' ? DARK_MAP_STYLE : undefined}
  initialRegion={initialRegion}
  showsUserLocation
  showsMyLocationButton={false}
  clusterColor="#3B82F6"          // <-- REMOVER (clustering-only)
  clusterTextColor="#FFFFFF"      // <-- REMOVER (clustering-only)
  clusterFontFamily={undefined}   // <-- REMOVER (clustering-only)
  radius={40}                     // <-- REMOVER (clustering-only)
  onClusterPress={() => {}}       // <-- REMOVER (clustering-only)
>
  {/* filhos permanecem intactos */}
</ClusteredMapView>
```

JSX destino (após a fix):
```tsx
<MapView
  ref={mapRef}
  style={StyleSheet.absoluteFillObject}
  provider={Platform.OS === 'android' ? PROVIDER_GOOGLE : PROVIDER_DEFAULT}
  mapType={currentStyleConfig.mapType}
  customMapStyle={mapStyle === 'escuro' ? DARK_MAP_STYLE : undefined}
  initialRegion={initialRegion}
  showsUserLocation
  showsMyLocationButton={false}
>
  {/* filhos idênticos ao estado atual */}
</MapView>
```

Dependências obsoletas em package.json a remover:
- "react-native-map-clustering": "^4.0.0"
- "leaflet": "1.9.4"
- "leaflet.heat": "0.2.0"
- "leaflet.markercluster": "1.5.3"

react-native-webview NÃO tem outros usos no app (grep confirmado) — pode ficar ou ser removido; deixar como está para não introduzir risco desnecessário nesta fase.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Substituir ClusteredMapView por MapView em mapas.tsx</name>
  <files>app/(panel)/mapas.tsx</files>
  <action>
Editar app/(panel)/mapas.tsx com as seguintes mudanças cirúrgicas:

1. Remover o import do react-native-map-clustering (linha 7):
   - REMOVER: `import ClusteredMapView from 'react-native-map-clustering';`
   - O import do MapView na linha 6 já está presente e correto — não alterar.

2. No JSX (bloco return, dentro de `<View style={styles.container}>`):
   - Substituir a tag de abertura `<ClusteredMapView` por `<MapView`
   - Substituir a tag de fechamento `</ClusteredMapView>` por `</MapView>`
   - Remover as cinco props exclusivas do clustering:
     * `clusterColor="#3B82F6"`
     * `clusterTextColor="#FFFFFF"`
     * `clusterFontFamily={undefined}`
     * `radius={40}`
     * `onClusterPress={() => {}}`
   - MANTER intactos: `ref`, `style`, `provider`, `mapType`, `customMapStyle`,
     `initialRegion`, `showsUserLocation`, `showsMyLocationButton`
   - MANTER intactos todos os filhos: o bloco `{!showHeatmap && filteredMarkers.map(...)}` e
     o bloco `{showHeatmap && Platform.OS === 'android' && (<Heatmap .../>)}`

3. Não alterar nenhuma outra linha do arquivo. Toda a lógica de dados (loadMarkers, filtros,
   popup, modal de estilo, FABs, header, legenda) permanece inalterada.
  </action>
  <verify>
    <automated>npx jest --passWithNoTests</automated>
  </verify>
  <done>
    - Arquivo não contém mais `ClusteredMapView` nem `react-native-map-clustering`
    - Arquivo contém `&lt;MapView` como root do mapa com as props corretas
    - `npx jest --passWithNoTests` passa sem erros de compilação TypeScript
  </done>
</task>

<task type="auto">
  <name>Task 2: Remover dependências obsoletas do package.json</name>
  <files>package.json</files>
  <action>
Editar package.json removendo as quatro entradas da seção `"dependencies"`:

1. `"react-native-map-clustering": "^4.0.0"` — biblioteca problemática que causava a tela branca
2. `"leaflet": "1.9.4"` — biblioteca WebView abandonada (fase 06 migrou para react-native-maps)
3. `"leaflet.heat": "0.2.0"` — plugin leaflet, sem uso no app
4. `"leaflet.markercluster": "1.5.3"` — plugin leaflet, sem uso no app

NÃO remover: `react-native-webview` (13.15.0) — pode ter usos fora do mapa.
NÃO remover: `react-native-maps` (1.20.1) — biblioteca principal que FICA.
NÃO alterar nenhuma outra entrada.

Após editar o arquivo, rodar:
```bash
npm install
```
para atualizar o package-lock.json. Em seguida rodar:
```bash
npx expo install --fix
```
para garantir que não há versões conflitantes.
  </action>
  <verify>
    <automated>npx jest --passWithNoTests</automated>
  </verify>
  <done>
    - package.json não contém mais `react-native-map-clustering`, `leaflet`, `leaflet.heat`, `leaflet.markercluster`
    - `react-native-maps` ainda presente em 1.20.1
    - `npx jest --passWithNoTests` continua passando
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificar mapa funcional no dispositivo</name>
  <action>Abrir o app no Expo Go e verificar que o mapa renderiza sem tela branca com marcadores corretos.</action>
  <verify>
    <automated>npx jest --passWithNoTests</automated>
  </verify>
  <done>Mapa visível no dispositivo, marcadores carregam com cores de risco, filtros e interações funcionam.</done>
  <what-built>
    Substituição de ClusteredMapView por MapView padrão em mapas.tsx + remoção de dependências obsoletas do package.json.
  </what-built>
  <how-to-verify>
    1. Abra o app no Expo Go em um dispositivo Android
    2. Faça login como agente
    3. Navegue até a tela de mapa (Mapa Tático)
    4. Verifique: o mapa renderiza sem tela branca — tiles do Google Maps aparecem
    5. Verifique: marcadores de vistoria aparecem com cores de risco (vermelho/laranja/amarelo/verde)
    6. Teste o filtro por risco (Alto / Médio / Baixo / Todos) — marcadores mudam
    7. Teste o botão de heatmap (ícone de raio) — heatmap ativa/desativa
    8. Toque em um marcador — popup de detalhes aparece com endereço e link "Ver detalhes"
    9. Teste o botão de estilo do mapa — modal abre com opções Padrão / Satélite / Relevo / Escuro
    10. Se disponível: repita os passos 3-9 em iOS

    Critério de aprovação: mapa visível, marcadores carregam, interações funcionam.
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva os problemas encontrados</resume-signal>
</task>

</tasks>

<verification>
- `app/(panel)/mapas.tsx` não importa nem usa `ClusteredMapView` ou `react-native-map-clustering`
- `app/(panel)/mapas.tsx` usa `<MapView>` como componente raiz do mapa com `provider={Platform.OS === 'android' ? PROVIDER_GOOGLE : PROVIDER_DEFAULT}`
- `package.json` não contém `react-native-map-clustering`, `leaflet`, `leaflet.heat`, `leaflet.markercluster`
- `npx jest --passWithNoTests` passa
- Verificação humana no dispositivo confirma mapa funcional
</verification>

<success_criteria>
- MAPA-01: Agente abre a tela de mapa e vê tiles do Google Maps renderizados sem tela branca no Android
- MAPA-02: Marcadores de vistoria aparecem no mapa com coordenadas corretas e cores por nível de risco
- Codebase sem `react-native-map-clustering` (4.0.0) — biblioteca problemática eliminada
- Codebase sem leaflet e plugins (WebView era fase anterior, agora obsoleta)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/11-mapa-nativo/11-01-SUMMARY.md` com:
- O que foi feito (mudanças em mapas.tsx e package.json)
- Confirmação de que ClusteredMapView foi removido
- Resultado da verificação humana no dispositivo
- Quaisquer problemas encontrados e como foram resolvidos
</output>
