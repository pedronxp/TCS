---
phase: 10-formul-rio-estrutural-inteligente
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(panel)/inspecoes/wizard.tsx
autonomous: true
requirements:
  - FORM-04

must_haves:
  truths:
    - "Wizard aceita campo skipSe no JSON sem quebrar formulários existentes (v1, deslizamento)"
    - "Perguntas com skipSe correspondente ao estado atual ficam invisíveis e não bloqueiam avanço"
    - "calcularNivelRisco não contabiliza respostas de perguntas fora de perguntasVisiveis"
    - "finalizar() não exige resposta de perguntas invisíveis"
    - "Header exibe progresso por elemento (ELEMENTO X/7) quando formulario usa skipSe"
  artifacts:
    - path: "app/(panel)/inspecoes/wizard.tsx"
      provides: "Interface SkipSe, campo skipSe em PerguntaModel, computado perguntasVisiveis"
      contains: "SkipSe"
  key_links:
    - from: "flattenPerguntas()"
      to: "PerguntaModel.skipSe"
      via: "propagacao do campo skipSe do JSON para o model"
      pattern: "skipSe"
    - from: "perguntasVisiveis"
      to: "calcularNivelRisco"
      via: "passagem de perguntasVisiveis como argumento"
      pattern: "perguntasVisiveis"
---

<objective>
Adicionar suporte a lógica condicional (skipSe) no wizard de avaliação, permitindo que perguntas sejam ocultadas dinamicamente com base em respostas anteriores. Também atualizar o header para exibir progresso por elemento em vez de por pergunta numérica.

Purpose: Habilitar o formulário estrutural v2 com skip automático quando Estado = "Bom", reduzindo de 60 para no maximo 7 perguntas em vistorias sem danos.

Output: wizard.tsx modificado com interface SkipSe, useMemo perguntasVisiveis, calcularNivelRisco e finalizar() atualizados, e label de progresso por elemento no header.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/(panel)/inspecoes/wizard.tsx
</context>

<interfaces>
<!-- Contratos atuais do wizard.tsx relevantes para este plano -->

Linha 70-81 — PerguntaModel atual (sem skipSe):
```typescript
interface PerguntaModel {
  id: string;
  texto: string;
  faseId?: string;
  grupo?: string;
  instrucao?: string;
  tipo: 'cards' | 'multipla_escolha' | 'texto' | 'foto';
  layout?: string;
  imagemExemplo?: string | null;
  obrigatoria: boolean;
  opcoes: OpcaoModel[];
  // skipSe AUSENTE — adicionar aqui
}
```

Linha 199-225 — flattenPerguntas() atual:
```typescript
const flattenPerguntas = (json: any): PerguntaModel[] => {
  // ... propaga id, texto, faseId, grupo, instrucao, tipo, imagemExemplo, obrigatoria, opcoes
  // NÃO propaga skipSe — adicionar: skipSe: p.skipSe || null
};
```

Linha 238-309 — calcularNivelRisco() atual:
```typescript
// Na branch ponderada_max_elemento (linha 250):
perguntas.forEach(p => { ... });  // deve virar: perguntasVisiveis.forEach(...)
// Na branch soma_total (linha 285):
perguntas.forEach(p => { ... });  // deve virar: perguntasVisiveis.forEach(...)
// Assinatura deve aceitar perguntasVisiveis como argumento
```

Linha 342-350 — finalizar() atual:
```typescript
const pendente = perguntas.find(p => p.obrigatoria && !respostas[p.id]);
// deve virar:
const pendente = perguntasVisiveis.find(p => p.obrigatoria && !respostas[p.id]);
```

Linha 227-229 — derivados de perguntas usados na UI:
```typescript
const perguntaAtual = perguntas[step];      // -> perguntasVisiveis[step]
const totalPerguntas = perguntas.length;    // -> perguntasVisiveis.length
const progress = totalPerguntas > 0 ? ((step + 1) / totalPerguntas) : 0;
```

Linha 453 — avancar() usa totalPerguntas:
```typescript
if (step < totalPerguntas - 1) setStep(s => s + 1);
// totalPerguntas ja sera de perguntasVisiveis, sem mudanca adicional
```

Linha 474 — header label atual:
```typescript
<Text>PASSO 3 DE 3 · PERGUNTA {step + 1}/{totalPerguntas}</Text>
// nova logica: se formulario tem faseId nas perguntas, exibir ELEMENTO X/Y
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Tarefa 1: Interface SkipSe + flattenPerguntas + perguntasVisiveis</name>
  <files>app/(panel)/inspecoes/wizard.tsx</files>
  <action>
Realizar tres alteracoes cirurgicas no wizard.tsx — sem reescrever o arquivo:

**1. Adicionar interface SkipSe e campo skipSe em PerguntaModel** (apos linha 81):
```typescript
interface SkipSe {
  perguntaId: string;
  opcaoId: string;
}

interface PerguntaModel {
  // ... campos existentes ...
  skipSe?: SkipSe | null;   // NOVO — pula esta pergunta se respostas[perguntaId] === opcaoId
}
```

**2. Propagar skipSe em flattenPerguntas()** — dentro do loop `for (const p of pergs)`, no objeto passado a `result.push({...})`, adicionar apos o campo `opcoes`:
```typescript
skipSe: p.skipSe || null,
```

**3. Criar computado perguntasVisiveis via useMemo** — adicionar apos a declaracao de `perguntas` state (apos linha 92):
```typescript
const perguntasVisiveis = useMemo(() =>
  perguntas.filter(p => {
    if (!p.skipSe) return true;
    const resposta = respostas[p.skipSe.perguntaId];
    return resposta !== p.skipSe.opcaoId;
  }),
  [perguntas, respostas]
);
```

**4. Substituir todas as referencias a `perguntas[step]` e `perguntas.length` na UI** pelas variaveis de perguntasVisiveis:
- `const perguntaAtual = perguntas[step]` → `const perguntaAtual = perguntasVisiveis[step]`
- `const totalPerguntas = perguntas.length` → `const totalPerguntas = perguntasVisiveis.length`
- Nao alterar `perguntas` usado DENTRO de flattenPerguntas ou fetchPerguntas — apenas os derivados de apresentacao

**Atencao:** O useMemo de `riscoAtual` (linha 312) ja depende de `[respostas, perguntas, limites, tipoCalculo, faseConfigs]` — ele chama `calcularNivelRisco()` que sera atualizado na tarefa 2. Nao alterar o array de dependencias do riscoAtual agora.
  </action>
  <verify>
    <automated>cd /c/Users/User/Desktop/Projeto/app_defesa_civil_expo && rtk npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
- Interface SkipSe declarada e PerguntaModel tem campo skipSe?: SkipSe | null
- flattenPerguntas() propaga skipSe: p.skipSe || null
- perguntasVisiveis computado via useMemo filtrando por respostas
- perguntaAtual e totalPerguntas derivados de perguntasVisiveis
- TypeScript compila sem erros nos arquivos alterados
  </done>
</task>

<task type="auto" tdd="false">
  <name>Tarefa 2: calcularNivelRisco, finalizar e progresso por elemento</name>
  <files>app/(panel)/inspecoes/wizard.tsx</files>
  <action>
**1. Atualizar calcularNivelRisco para usar perguntasVisiveis**

A funcao atual itera `perguntas` em dois pontos. Converter a assinatura para aceitar a lista filtrada como argumento (evita closure inconsistente no useMemo do riscoAtual):

```typescript
const calcularNivelRisco = (visiveis: PerguntaModel[] = perguntasVisiveis): { nivel: string; pontuacao: number } => {
  // ...
  // branch ponderada_max_elemento: substituir
  //   perguntas.forEach(p => {
  // por:
  //   visiveis.forEach(p => {

  // branch soma_total: substituir
  //   perguntas.forEach(p => {
  // por:
  //   visiveis.forEach(p => {
};
```

Atualizar o useMemo de riscoAtual para passar perguntasVisiveis explicitamente e adicionar perguntasVisiveis no array de dependencias:
```typescript
const riscoAtual = useMemo(() => {
  if (Object.keys(respostas).length === 0) return null;
  return calcularNivelRisco(perguntasVisiveis);
}, [respostas, perguntasVisiveis, limites, tipoCalculo, faseConfigs]);
```

Na chamada dentro de `finalizar()`:
```typescript
const { nivel, pontuacao } = calcularNivelRisco(perguntasVisiveis);
```

**2. Corrigir finalizar() para verificar somente perguntas visiveis**

Linha 344: substituir `perguntas.find(...)` por `perguntasVisiveis.find(...)`:
```typescript
const pendente = perguntasVisiveis.find(p => p.obrigatoria && !respostas[p.id]);
if (pendente) {
  Alert.alert('Pergunta obrigatoria', `Responda: "${pendente.texto}"`);
  const idx = perguntasVisiveis.indexOf(pendente);   // indice em visiveis, nao em perguntas
  setStep(idx);
  return;
}
```

**3. Atualizar label de progresso no header**

Substituir o Text do stepLabel (linha 474) para exibir progresso por elemento quando o formulario tem fases distintas:

```typescript
// Calcular elemento atual a partir da pergunta visivel atual
const elementoAtual = useMemo(() => {
  const p = perguntasVisiveis[step];
  if (!p?.faseId) return null;
  const fasesUnicas = [...new Set(perguntasVisiveis.map(x => x.faseId).filter(Boolean))];
  const idx = fasesUnicas.indexOf(p.faseId);
  return idx >= 0 ? { atual: idx + 1, total: fasesUnicas.length } : null;
}, [perguntasVisiveis, step]);
```

No JSX do header, substituir o Text do stepLabel:
```tsx
<Text style={[styles.stepLabel, { color: theme.textSecondary }]}>
  PASSO 3 DE 3 ·{' '}
  {elementoAtual
    ? `ELEMENTO ${elementoAtual.atual}/${elementoAtual.total}`
    : `PERGUNTA ${step + 1}/${totalPerguntas}`}
</Text>
```

Isso mantem retrocompatibilidade: formularios sem faseId (perguntas planas) continuam exibindo "PERGUNTA X/N".
  </action>
  <verify>
    <automated>cd /c/Users/User/Desktop/Projeto/app_defesa_civil_expo && rtk npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
- calcularNivelRisco aceita argumento visiveis e usa-o internamente em ambas as branches
- useMemo riscoAtual passa perguntasVisiveis explicitamente e tem dependencia correta
- finalizar() busca pendente em perguntasVisiveis e usa indexOf(pendente) para setStep
- Header exibe "ELEMENTO X/Y" quando ha faseId, "PERGUNTA X/N" quando nao ha
- TypeScript compila sem erros em todo o arquivo
  </done>
</task>

</tasks>

<verification>
- Abrir formulario risco_estrutural_v1 (sem skipSe) → deve funcionar identico ao antes (60 perguntas, PERGUNTA X/60 no header)
- Abrir formulario vistoria_deslizamento_v1 → deve funcionar identico ao antes
- TypeScript sem erros: `npx tsc --noEmit`
- Grep confirma skipSe propagado: `grep -n "skipSe" app/(panel)/inspecoes/wizard.tsx` deve retornar ao menos 5 ocorrencias (interface, PerguntaModel, flattenPerguntas, useMemo, calcularNivelRisco)
</verification>

<success_criteria>
- Interface SkipSe e campo PerguntaModel.skipSe existem em wizard.tsx
- perguntasVisiveis e elementoAtual sao computados via useMemo
- calcularNivelRisco, finalizar e avancar usam perguntasVisiveis
- Formularios existentes (v1, deslizamento) continuam funcionando sem regressao
- TypeScript compila sem erros
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/10-formul-rio-estrutural-inteligente/10-01-SUMMARY.md`
</output>
