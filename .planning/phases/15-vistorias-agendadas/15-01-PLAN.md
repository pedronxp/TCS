---
plan: 15-01
phase: 15-vistorias-agendadas
status: pending
goal: Criar tabela de agendamentos no SQLite + Supabase e tipos compartilhados
---

## Goal

Estrutura de dados para vistorias agendadas: tabela SQLite local + tabela Supabase + tipos TypeScript compartilhados.

## Schema

### SQLite (utils/database.ts)

Nova tabela `agendamentos` adicionada na migração (version bump para 5):

```sql
CREATE TABLE IF NOT EXISTS agendamentos (
  id TEXT PRIMARY KEY,
  titulo TEXT NOT NULL,
  endereco TEXT,
  municipio TEXT NOT NULL,
  data_agendada TEXT NOT NULL,   -- ISO8601
  criado_por_uid TEXT NOT NULL,
  criado_por_nome TEXT,
  agente_uid TEXT,               -- NULL = sem agente atribuído
  agente_nome TEXT,
  lat REAL,
  lng REAL,
  observacoes TEXT,
  status TEXT DEFAULT 'pendente', -- pendente | concluido | cancelado
  criado_em TEXT,
  sincronizado INTEGER DEFAULT 0
)
```

### Supabase

Migration SQL em `supabase/migrations/`:
```sql
CREATE TABLE IF NOT EXISTS agendamentos (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  titulo TEXT NOT NULL,
  endereco TEXT,
  municipio TEXT NOT NULL,
  data_agendada TIMESTAMPTZ NOT NULL,
  criado_por_uid UUID REFERENCES users(uid),
  criado_por_nome TEXT,
  agente_uid UUID REFERENCES users(uid),
  agente_nome TEXT,
  lat DOUBLE PRECISION,
  lng DOUBLE PRECISION,
  observacoes TEXT,
  status TEXT DEFAULT 'pendente' CHECK (status IN ('pendente','concluido','cancelado')),
  criado_em TIMESTAMPTZ DEFAULT NOW()
);

-- RLS: agentes só veem agendamentos do seu município
ALTER TABLE agendamentos ENABLE ROW LEVEL SECURITY;
CREATE POLICY "agentes_veem_municipio" ON agendamentos
  FOR SELECT USING (municipio = (SELECT municipio FROM users WHERE uid = auth.uid()));
CREATE POLICY "admin_gerencia" ON agendamentos
  FOR ALL USING (
    (SELECT role FROM users WHERE uid = auth.uid()) IN ('admin','supervisor','master_admin')
  );
```

## Tasks

### Task 1 — utils/database.ts: adicionar tabela agendamentos

Adicionar migração v5 no bloco `withTransactionSync`:
```ts
if (currentVersion < 5) {
  database.runSync(`CREATE TABLE IF NOT EXISTS agendamentos (
    id TEXT PRIMARY KEY,
    titulo TEXT NOT NULL,
    endereco TEXT,
    municipio TEXT NOT NULL,
    data_agendada TEXT NOT NULL,
    criado_por_uid TEXT NOT NULL,
    criado_por_nome TEXT,
    agente_uid TEXT,
    agente_nome TEXT,
    lat REAL,
    lng REAL,
    observacoes TEXT,
    status TEXT DEFAULT 'pendente',
    criado_em TEXT,
    sincronizado INTEGER DEFAULT 0
  )`);
  database.runSync(`UPDATE schema_version SET version = 5`);
}
```

Adicionar funções CRUD:
- `insertAgendamento(a: AgendamentoLocal): void`
- `getAgendamentosByMunicipio(municipio: string): AgendamentoLocal[]`
- `getAgendamentosByAgente(agenteUid: string): AgendamentoLocal[]`
- `updateAgendamentoStatus(id: string, status: string): void`

### Task 2 — types/agendamento.ts (novo arquivo)

```ts
export interface AgendamentoLocal {
  id: string;
  titulo: string;
  endereco?: string;
  municipio: string;
  data_agendada: string;
  criado_por_uid: string;
  criado_por_nome?: string;
  agente_uid?: string;
  agente_nome?: string;
  lat?: number;
  lng?: number;
  observacoes?: string;
  status: 'pendente' | 'concluido' | 'cancelado';
  criado_em?: string;
  sincronizado?: number;
}
```

### Task 3 — supabase/migrations/: criar arquivo SQL

Criar `supabase/migrations/20260403_agendamentos.sql` com o DDL completo descrito acima.

## Verification

- [ ] App inicia sem crash (migração v5 roda)
- [ ] `agendamentos` tabela existe no SQLite após cold start
- [ ] Tipos TypeScript exportados corretamente de `types/agendamento.ts`
