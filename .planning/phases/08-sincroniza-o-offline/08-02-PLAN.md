---
phase: 08-sincroniza-o-offline
plan: "02"
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - services/__tests__/SyncService.test.ts
autonomous: true
requirements:
  - SYNC-01
must_haves:
  truths:
    - "Teste automatizado confirma que vistoria pendente é enviada e marcada como sincronizada após syncPendentes()"
    - "Teste confirma que o mesmo id nunca gera duplicata no Supabase (upsert chamado uma vez por id por sync)"
    - "Teste confirma que foto file:// dispara upload via StorageService antes do upsert"
    - "Teste confirma que falha de rede incrementa tentativas e agenda retry com backoff"
    - "Teste confirma que vistoria com 5 tentativas esgotadas não é enviada nem incrementada"
  artifacts:
    - path: "services/__tests__/SyncService.test.ts"
      provides: "Suite completa cobrindo fluxos de SYNC-01"
      min_lines: 120
      exports: []
  key_links:
    - from: "services/__tests__/SyncService.test.ts"
      to: "services/SyncService.ts"
      via: "require('../SyncService') + mocks de database e supabase"
      pattern: "require.*SyncService"
---

<objective>
Expandir a suite de testes de SyncService.ts de 4 para 9 casos, cobrindo os fluxos de SYNC-01 que não têm evidência automatizada: deduplicação, upload de foto offline, falha de rede com backoff, e vistoria esgotada.

Purpose: SYNC-01 exige que o comportamento seja demonstrável de forma confiável. Os 4 testes existentes cobrem apenas o caminho feliz básico e a guarda de concorrência. Os 5 novos casos cobrem os cenários de erro e edge cases que determinam se o requisito realmente está atendido.
Output: services/__tests__/SyncService.test.ts com 9 casos, todos verdes.
</objective>

<execution_context>
@/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/08-sincroniza-o-offline/08-RESEARCH.md
@.planning/phases/08-sincroniza-o-offline/08-01-SUMMARY.md

Arquivos a modificar:
@services/__tests__/SyncService.test.ts

Arquivos de referência (ler, não modificar):
@services/SyncService.ts
@services/StorageService.ts
@utils/database.ts
</context>

<interfaces>
<!-- Contratos relevantes para escrever os mocks -->

De services/SyncService.ts — exports públicos:
```typescript
export async function syncPendentes(isRetry?: boolean): Promise<{ sucesso: number; falha: number }>
export function cancelAutoRetry(): void
export async function registerBackgroundSync(): Promise<void>
export function startAppStateSyncListener(): void
export function stopAppStateSyncListener(): void
```

Estrutura de VistoriaLocal usada nos testes (já existe em makeVistoria() no arquivo de testes):
```typescript
{
  id: string,                // UUID — chave de deduplicação
  agente_uid: string,
  agente_nome: string,
  municipio: string,
  endereco_rua: string, endereco_numero: string, endereco_bairro: string,
  endereco_cep: null,
  responsavel_nome: null,
  latitude: number, longitude: number,
  data_vistoria: string,     // ISO 8601
  formulario_id: string, formulario_versao: number,
  respostas_json: string,    // JSON string
  nivel_risco: string,       // 'r1'|'r2'|'r3'|'r4'
  pontuacao_total: number,
  foto_url: string | null,   // 'file://' ou 'https://' ou null
  fotos_urls: string | null, // JSON string de array ou null
  sincronizado: 0 | 1,
  erro_sync: string | null,
  tentativas_sync: number,
  criado_em: string,
}
```

Mocks já configurados no topo do arquivo de testes:
- `jest.mock('../../utils/supabase')` — supabase.from().upsert retorna { error: null }
- `jest.mock('../../utils/database')` — mockGetVistoriasNaoSincronizadas, mockMarkSincronizado, etc.
- `jest.mock('expo-task-manager')`, `jest.mock('expo-background-fetch')`
- `jest.mock('../../services/NotificationService')`

Para os novos testes, adicionar mock de StorageService:
```typescript
jest.mock('../../services/StorageService', () => ({
  uploadImageFromLocalUri: jest.fn().mockResolvedValue('https://storage.example.com/foto.jpg'),
}));
```

Padrão de require dentro do teste (necessário por causa de jest.resetModules() no beforeEach):
```typescript
const { syncPendentes } = require('../SyncService');
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Adicionar 5 novos casos de teste em SyncService.test.ts</name>
  <files>services/__tests__/SyncService.test.ts</files>
  <behavior>
    Caso 1 — Deduplicação: syncPendentes() chamada duas vezes com o mesmo id não chama upsert duas vezes para o mesmo registro. Na segunda chamada, getVistoriasNaoSincronizadas retorna lista vazia (pois o primeiro sync já marcou como sincronizado). upsert é chamado exatamente 1 vez.

    Caso 2 — Upload de foto file://: vistoria com foto_url='file:///cache/foto.jpg' dispara uploadImageFromLocalUri antes do upsert. uploadImageFromLocalUri é chamado com a URI local. O payload enviado ao upsert contém a URL pública (https://), não a URI local.

    Caso 3 — Falha de upsert incrementa tentativas: supabase.from().upsert retorna { error: { message: 'network error' } }. Após syncPendentes(), mockIncrementTentativas é chamado com o id da vistoria. mockMarkErroSync é chamado com o id e a mensagem de erro.

    Caso 4 — Backoff com fake timers: após falha, scheduleAutoRetry agenda retry em 30s. Usar jest.useFakeTimers(). Após jest.advanceTimersByTime(29_999), syncPendentes NÃO foi chamada de novo. Após jest.advanceTimersByTime(1), syncPendentes FOI chamada de novo. Cleanup: jest.useRealTimers().

    Caso 5 — Vistoria esgotada (tentativas_sync=5) é ignorada sem incrementar: getVistoriasNaoSincronizadas retorna vistoria com tentativas_sync=5. Resultado é { sucesso:0, falha:0 }. mockIncrementTentativas NÃO é chamado. mockMarkErroSync NÃO é chamado. (Este cenário é parcialmente coberto mas o assert sobre não-incremento está ausente nos testes atuais.)
  </behavior>
  <action>
Adicionar ao final do arquivo services/__tests__/SyncService.test.ts, dentro do describe('syncPendentes'), os 5 novos casos de teste.

Antes de adicionar os casos, incluir no topo do arquivo (junto aos outros jest.mock):
```typescript
jest.mock('../../services/StorageService', () => ({
  uploadImageFromLocalUri: jest.fn().mockResolvedValue('https://storage.example.com/foto.jpg'),
}));
```

Para o Caso 4 (backoff), como `scheduleAutoRetry` é privado, a forma de testar é:
- Mock `supabase.from().upsert` para retornar erro na primeira chamada de syncPendentes
- Usar jest.useFakeTimers() ANTES de chamar syncPendentes
- Capturar a contagem de chamadas do mock upsert antes e depois de avançar o timer
- Verificar que após 30s, upsert foi chamado novamente (retry disparou)
- Usar jest.useRealTimers() no teardown do caso (finally ou afterEach)

Atenção: os mocks de supabase precisam ser re-configurados dentro de cada teste quando o comportamento varia (erro vs sucesso). Usar:
```typescript
const { supabase } = require('../../utils/supabase');
supabase.from.mockReturnValue({
  upsert: jest.fn().mockResolvedValue({ error: { message: 'network error' } }),
});
```

Manter o beforeEach existente que faz jest.resetModules() e limpa os mocks.

Não apagar nem modificar os 4 testes existentes.
  </action>
  <verify>
    <automated>npx jest services/__tests__/SyncService.test.ts --no-coverage --verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
Suite tem 9 casos. Todos passam (0 falhas). Output de --verbose mostra os 5 novos nomes de teste listados. Cobertura de syncPendentes cobre: fila vazia, sucesso simples, tentativas esgotadas, concorrência, deduplicação, upload de foto, falha com incremento, backoff, esgotado sem incremento.
  </done>
</task>

</tasks>

<verification>
Após a tarefa:

1. `npx jest services/__tests__/SyncService.test.ts --no-coverage --verbose` — 9 testes, 0 falhas
2. `npm test 2>&1 | tail -20` — suite geral ainda passa (sem regressões)
3. Confirmar que os 5 novos casos correspondem aos 5 gaps identificados na pesquisa (seção "Wave 0 Gaps" do 08-RESEARCH.md)
</verification>

<success_criteria>
- 9 casos de teste em SyncService.test.ts, todos verdes
- Cobertos: deduplicação por UUID, upload file://, falha com incremento de tentativas, backoff de 30s, esgotado sem incremento
- Nenhum teste existente foi quebrado ou removido
- Suite geral (`npm test`) continua verde
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/08-sincroniza-o-offline/08-02-SUMMARY.md` seguindo o template em @$HOME/.claude/get-shit-done/templates/summary.md
</output>
