---
phase: 06-mapa-autentica-o
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ConnectivityBanner.tsx
autonomous: false
gap_closure: true
requirements:
  - MAPA-02

must_haves:
  truths:
    - "Banner offline é visualmente integrado ao design do app — não tapa o header"
    - "Banner usa cores do design system (warning/success tokens) com dark mode automático"
  artifacts:
    - path: "components/ConnectivityBanner.tsx"
      provides: "Pill/toast flutuante centralizado usando tokens do design system com suporte a dark mode"
      contains: "useColorScheme"
  key_links:
    - from: "components/ConnectivityBanner.tsx"
      to: "constants/Colors.ts"
      via: "Colors[scheme].warning, warningLight, warningText, success, successLight, successText"
      pattern: "Colors\\[scheme\\]"
---

<objective>
Fecha gap cosmético do UAT teste 3: o ConnectivityBanner atual é uma barra full-width `position: absolute, top: 0` que tampa o header das telas com cores hardcoded (#F59E0B, #10B981) sem dark mode.

Fix: converter para pill/toast flutuante centralizado posicionado abaixo do safe area (não sobre o header), usando tokens do design system já presentes em constants/Colors.ts.

Purpose: Banner integrado visualmente ao design system sem quebrar a navegação.
Output: components/ConnectivityBanner.tsx refatorado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

<interfaces>
<!-- Tokens disponíveis em constants/Colors.ts para uso no banner -->

Light mode (Colors.light):
```typescript
warning: '#D97706',         // cor do ícone e borda (offline)
warningLight: '#FFFBEB',    // fundo do pill (offline)
warningText: '#78350F',     // texto sobre warningLight — 8.1:1 AA

success: '#16A34A',         // cor do ícone e borda (restaurado)
successLight: '#F0FDF4',    // fundo do pill (restaurado)
successText: '#14532D',     // texto sobre successLight — 7.3:1 AA
```

Dark mode (Colors.dark):
```typescript
warning: '#FCD34D',
warningLight: 'rgba(252,211,77,0.12)',
warningText: '#FEF3C7',

success: '#4ADE80',
successLight: 'rgba(74,222,128,0.12)',
successText: '#BBF7D0',
```

Hook de tema (padrão do projeto):
```typescript
import { useColorScheme } from 'react-native';
const scheme = useColorScheme() ?? 'light';
const colors = Colors[scheme];
```

API atual do ConnectivityBanner (não muda — sem props):
```typescript
export function ConnectivityBanner()  // sem props, mantido igual
```

Imports existentes no arquivo atual:
- React, useEffect, useRef, useState de 'react'
- Text, StyleSheet, Animated de 'react-native'
- useSafeAreaInsets de 'react-native-safe-area-context'
- Feather de '@expo/vector-icons'
- useConnectivity de '../context/ConnectivityContext'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refatorar ConnectivityBanner para pill flutuante com design system</name>
  <files>components/ConnectivityBanner.tsx</files>
  <action>
Reescrever components/ConnectivityBanner.tsx mantendo toda a lógica de animação/timing existente e alterando apenas aparência e posicionamento.

**Mudanças obrigatórias:**

1. Adicionar import `useColorScheme` de 'react-native' e `Colors` de '../constants/Colors'
2. Dentro do componente, adicionar:
   ```typescript
   const scheme = useColorScheme() ?? 'light';
   const colors = Colors[scheme];
   ```
3. Trocar `position: 'absolute', top: 0, left: 0, right: 0` por pill centralizado:
   - `position: 'absolute'`
   - `top: insets.top + 12` (abaixo do safe area, sem tampar o header)
   - `alignSelf: 'center'` (centralizado horizontalmente)
   - Remover `left: 0, right: 0`
4. Trocar dimensões de barra full-width por pill compacto:
   - Remover `height: BANNER_HEIGHT + insets.top` e `paddingTop: insets.top`
   - `paddingHorizontal: 16`, `paddingVertical: 8`
   - `borderRadius: 20`
   - `borderWidth: 1`
5. Substituir cores hardcoded por tokens do design system:
   - Offline: `backgroundColor: colors.warningLight`, `borderColor: colors.warning`
   - Restaurado: `backgroundColor: colors.successLight`, `borderColor: colors.success`
   - Ícone offline: `color: colors.warning` (não mais '#fff')
   - Ícone restaurado: `color: colors.success`
   - Texto offline: `color: colors.warningText`
   - Texto restaurado: `color: colors.successText`
6. Adicionar sombra sutil para o pill flutuar sobre o conteúdo:
   - `elevation: 4` (Android)
   - `shadowColor: '#000'`, `shadowOffset: { width: 0, height: 2 }`, `shadowOpacity: 0.12`, `shadowRadius: 4` (iOS)
7. Ajustar a animação de translateY inicial para refletir a nova posição:
   - O valor inicial negativo deve ser suficiente para esconder o pill (ex: `-80` é suficiente já que o pill é pequeno)
   - O `hideBanner` também deve usar o mesmo valor negativo

**Lógica que NÃO muda:**
- `useConnectivity()` e `isOnlineReal`
- `showBanner` / `hideBanner` com Animated.timing
- `hideTimer` de 2500ms para "Conexão restaurada"
- Textos: 'Modo Offline — dados sincronizados ao reconectar' e 'Conexão restaurada'
- Ícones: 'wifi-off' e 'wifi'
- Retorno `null` quando `bannerState === 'hidden'`
- Tamanho do ícone: 13
- `zIndex: 9999`
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | grep -i "ConnectivityBanner" || echo "sem erros de tipo no banner"</automated>
  </verify>
  <done>
    - components/ConnectivityBanner.tsx importa useColorScheme e Colors
    - Não contém mais '#F59E0B' nem '#10B981' (cores hardcoded removidas)
    - Contém `colors.warningLight` e `colors.successLight` como backgroundColor
    - Contém `top: insets.top + 12` (não mais `top: 0`)
    - Contém `borderRadius: 20` (pill)
    - Sem erros de TypeScript
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificar banner offline no app</name>
  <files>n/a — verificação visual no app, sem alterações de código</files>
  <action>
    Verificação visual do ConnectivityBanner refatorado no app em dispositivo ou emulador.

    1. Abrir o app em dispositivo ou emulador
    2. Ativar modo avião (ou desligar Wi-Fi + dados móveis)
    3. ESPERADO: pill compacto aparece abaixo do header/safe area (não tapa a navegação), fundo âmbar claro com texto escuro e ícone wifi-off, mensagem "Modo Offline — dados sincronizados ao reconectar"
    4. Reativar a conexão
    5. ESPERADO: pill muda para verde claro com texto "Conexão restaurada" e desaparece após ~2,5 segundos
    6. (Opcional) Testar em dark mode: pill deve usar fundo semitransparente âmbar/verde escuro com texto legível
    7. Confirmar que o banner NÃO tampa o header nem os botões de navegação
  </action>
  <verify>Banner pill visível e não sobrepõe o header — confirmação visual do usuário</verify>
  <done>
    - Banner offline exibe pill flutuante abaixo do header (não sobre ele)
    - Cores âmbar/verde integradas ao design system do app
    - Animação de entrada/saída mantida
  </done>
  <resume-signal>Digite "aprovado" se o banner está visualmente correto, ou descreva o problema encontrado (screenshot ajuda)</resume-signal>
</task>

</tasks>

<verification>
- grep confirma ausência de '#F59E0B' e '#10B981' em ConnectivityBanner.tsx
- grep confirma presença de `colors.warningLight` e `colors.successLight`
- grep confirma `top: insets.top + 12` (pill abaixo do safe area)
- npx tsc --noEmit sem erros relacionados ao banner
- Verificação visual no app aprovada pelo usuário
</verification>

<success_criteria>
- Banner offline não tapa o header em nenhuma tela
- Cores do banner usam tokens do design system (warning/success)
- Dark mode funciona automaticamente sem hardcoded colors
- Animação de entrada/saída mantida
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/06-mapa-autentica-o/06-05-SUMMARY.md`
</output>
