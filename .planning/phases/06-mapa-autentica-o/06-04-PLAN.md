---
phase: 06-mapa-autentica-o
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
gap_closure: true
requirements:
  - AUTH-01

must_haves:
  truths:
    - "Registro com token recém-criado é aceito sem erro 'Token inválido ou já utilizado'"
  artifacts:
    - path: "supabase/functions/validate_invite_token (PostgreSQL)"
      provides: "Função SQL server-side que compara expiraEm com now() no fuso correto"
      contains: "validate_invite_token"
  key_links:
    - from: "app/(auth)/register.tsx"
      to: "supabase.rpc('validate_invite_token')"
      via: "função PostgreSQL deve existir no banco para RPC funcionar"
      pattern: "validate_invite_token"
---

<objective>
Fecha gap AUTH-01: a função PostgreSQL `validate_invite_token` nunca foi criada no Supabase. Sem ela, qualquer chamada `supabase.rpc('validate_invite_token')` retorna erro de "function does not exist", que register.tsx interpreta como token inválido — impedindo todo registro de novos agentes.

Purpose: Desbloquear o fluxo de onboarding. Nenhuma mudança de código é necessária — register.tsx já foi corrigido no plano 06-02.
Output: Função SQL aplicada no Supabase, fluxo de registro funcional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-mapa-autentica-o/06-02-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Aplicar função validate_invite_token no Supabase SQL Editor</name>
  <files>n/a — ação manual no Supabase Dashboard, sem arquivos locais</files>
  <action>
    Ação manual obrigatória — Claude não tem acesso ao Supabase SQL Editor.

    Abrir o Supabase Dashboard do projeto, ir em SQL Editor e executar os blocos SQL abaixo em sequência.

    **Bloco 1 — verificar tipo da coluna expiraEm (executar primeiro):**

    ```sql
    SELECT column_name, data_type
    FROM information_schema.columns
    WHERE table_name = 'invite_tokens' AND column_name = 'expiraEm';
    ```

    Se o resultado mostrar `timestamp without time zone`, executar este bloco antes de criar a função:

    ```sql
    ALTER TABLE invite_tokens
      ALTER COLUMN "expiraEm" TYPE TIMESTAMPTZ
      USING "expiraEm" AT TIME ZONE 'UTC';
    ```

    Se o resultado já mostrar `timestamp with time zone`, pular este ALTER e ir direto ao Bloco 2.

    **Bloco 2 — criar a função (obrigatório):**

    ```sql
    CREATE OR REPLACE FUNCTION validate_invite_token(p_codigo TEXT)
    RETURNS TABLE (
      id UUID,
      codigo TEXT,
      municipio TEXT,
      role TEXT,
      valido BOOLEAN,
      motivo TEXT
    ) AS $$
    BEGIN
      RETURN QUERY
      SELECT
        t.id,
        t.codigo,
        t.municipio,
        t.role,
        CASE
          WHEN t.usado THEN FALSE
          WHEN t."expiraEm" IS NOT NULL AND t."expiraEm" < now() THEN FALSE
          ELSE TRUE
        END AS valido,
        CASE
          WHEN t.usado THEN 'Token já utilizado.'
          WHEN t."expiraEm" IS NOT NULL AND t."expiraEm" < now() THEN 'Token expirado. Solicite um novo ao administrador.'
          ELSE 'ok'
        END AS motivo
      FROM invite_tokens t
      WHERE t.codigo = p_codigo AND t.usado = false
      LIMIT 1;
    END;
    $$ LANGUAGE plpgsql SECURITY DEFINER;
    ```

    **Bloco 3 — confirmar que a função foi criada:**

    ```sql
    SELECT routine_name, routine_type
    FROM information_schema.routines
    WHERE routine_name = 'validate_invite_token';
    ```

    Deve retornar uma linha com `validate_invite_token | FUNCTION`.
  </action>
  <verify>Bloco 3 retorna linha `validate_invite_token | FUNCTION` no SQL Editor</verify>
  <done>Função validate_invite_token existe no Supabase e é chamável via RPC</done>
  <resume-signal>Digite "sql aplicado" após executar os blocos com sucesso, ou descreva o erro exato que apareceu no SQL Editor</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificar fluxo completo de registro com token no app</name>
  <files>n/a — verificação funcional no app, sem alterações de código</files>
  <action>
    Verificação manual do fluxo de registro com token de convite no app após a função SQL estar ativa.

    1. Logar no app como admin ou master_admin
    2. Navegar até "Gerar Token" → selecionar perfil "Agente" → duração "24h" → gerar
    3. Anotar o código gerado (formato XXXX-XXXX-XXXX)
    4. Sair da conta (logout) ou abrir em outro dispositivo/emulador
    5. Na tela de login, tocar em "Criar conta" ou "Registrar"
    6. Inserir o código recém gerado → preencher nome, email e senha → confirmar
    7. ESPERADO: registro concluído sem erro "Token inválido ou já utilizado"
    8. ESPERADO: token expirado genuíno deve retornar "Token expirado. Solicite um novo ao administrador."
  </action>
  <verify>Registro com token recém-criado conclui sem erro na tela do app</verify>
  <done>
    - Token válido recém-criado aceito imediatamente pelo fluxo de registro
    - Token expirado retorna mensagem descritiva em português
  </done>
  <resume-signal>Digite "aprovado" se o fluxo de registro funcionou, ou descreva o erro exato que apareceu na tela</resume-signal>
</task>

</tasks>

<verification>
- SQL Editor do Supabase retorna linha `validate_invite_token | FUNCTION` (Bloco 3)
- Registro com token válido recém-criado conclui sem erro
- Registro com token expirado retorna mensagem em português do servidor
</verification>

<success_criteria>
- AUTH-01 fechado: token recém-criado aceito imediatamente no fluxo de registro
- Função validate_invite_token existe no Supabase e é chamável via RPC
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/06-mapa-autentica-o/06-04-SUMMARY.md`
</output>
