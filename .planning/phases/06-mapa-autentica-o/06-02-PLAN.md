---
phase: 06-mapa-autentica-o
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - utils/__tests__/tokenExpiry.test.ts
  - app/(auth)/register.tsx
autonomous: true
requirements:
  - AUTH-01

must_haves:
  truths:
    - "Admin cria token de convite e usuário consegue usá-lo imediatamente (no mesmo dia) sem receber 'Token expirado'"
    - "Token genuinamente expirado é rejeitado com mensagem em português"
    - "Token já utilizado é rejeitado com mensagem em português"
  artifacts:
    - path: "utils/__tests__/tokenExpiry.test.ts"
      provides: "Testes unitários para lógica de validação de token"
      exports: ["validate_invite_token response handling"]
    - path: "app/(auth)/register.tsx"
      provides: "Fluxo de registro usando RPC server-side para validação de token"
      contains: "validate_invite_token"
  key_links:
    - from: "app/(auth)/register.tsx (handleRegister)"
      to: "supabase.rpc('validate_invite_token', { p_codigo: codigoNorm })"
      via: "substituição da query .from('invite_tokens').select(...)"
      pattern: "validate_invite_token"
---

<objective>
Corrige AUTH-01: tokens de convite recém-criados aparecem como "expirados" porque a comparação de data é feita no cliente (JavaScript) onde diferenças de fuso horário ou drift de relógio causam falsos positivos.

Root cause: `register.tsx` linha 81 faz `new Date(tokenData.expiraEm) < new Date()`. Se o Supabase retorna `expiraEm` como string sem sufixo `Z` (coluna TIMESTAMP sem timezone), o JavaScript a interpreta como hora local — um token criado às 14h UTC-3 e armazenado como "17:00:00" sem Z pode aparecer expirado em outro fuso.

Fix: Substituir a query direta na tabela + verificação client-side por uma chamada ao RPC `validate_invite_token` que faz a comparação com `now()` do PostgreSQL. O RPC é documentado no RESEARCH.md com SQL completo.

Note: O RPC precisa existir no Supabase antes da execução deste plano. A Task 1 cria o teste unitário (Wave 0). A Task 2 modifica o register.tsx. O humano aplica o SQL do RPC no Supabase dashboard antes de testar.

Purpose: Desbloqueia o fluxo de onboarding de novos agentes.
Output: `register.tsx` usando RPC + test file cobrindo os cenários de validação.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-mapa-autentica-o/06-RESEARCH.md

<interfaces>
<!-- Código atual em register.tsx que será substituído (linhas 71-83) -->

ANTES (client-side — bugado):
```typescript
const { data: tokenData, error: tokenError } = await supabase
  .from('invite_tokens')
  .select('id, codigo, expiraEm, municipio, role, usado')
  .eq('codigo', codigoNorm)
  .eq('usado', false)
  .single();

if (tokenError || !tokenData) throw new Error('Token inválido ou já utilizado.');

// Verificar expiração
if (tokenData.expiraEm && new Date(tokenData.expiraEm) < new Date()) {
  throw new Error('Token expirado. Solicite um novo ao administrador.');
}
```

DEPOIS (server-side — correto):
```typescript
const { data: tokenValidation, error: validationError } = await supabase
  .rpc('validate_invite_token', { p_codigo: codigoNorm })
  .single();

if (validationError || !tokenValidation) throw new Error('Token inválido ou já utilizado.');
if (!tokenValidation.valido) throw new Error(tokenValidation.motivo);

// tokenValidation.municipio e tokenValidation.role estão disponíveis diretamente
const tokenData = tokenValidation; // compatibilidade com o restante do código
```

Tipo retornado pelo RPC (para testes):
```typescript
interface TokenValidation {
  id: string;
  codigo: string;
  municipio: string;
  role: string;
  valido: boolean;
  motivo: string;
}
```

O restante de handleRegister (criação de conta, insert em users, delete do token) PERMANECE INALTERADO.
O `tokenData.municipio` e `tokenData.role` continuam acessíveis via `tokenValidation.municipio` e `tokenValidation.role`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — Criar teste unitário para lógica de validação de token via RPC</name>
  <files>utils/__tests__/tokenExpiry.test.ts</files>
  <read_first>
    - utils/__tests__/logger.test.ts (ver padrão de mocks usado nos testes existentes)
    - utils/__tests__/risco.test.ts (ver estrutura de teste existente)
  </read_first>
  <behavior>
    - Cenário 1: RPC retorna `{ valido: true, motivo: 'ok', municipio: 'SP', role: 'agent' }` → deve NÃO lançar erro
    - Cenário 2: RPC retorna `{ valido: false, motivo: 'Token expirado. Solicite um novo ao administrador.' }` → deve lançar Error com esse motivo
    - Cenário 3: RPC retorna `{ valido: false, motivo: 'Token já utilizado.' }` → deve lançar Error com esse motivo
    - Cenário 4: RPC retorna erro (validationError truthy) → deve lançar 'Token inválido ou já utilizado.'
    - Cenário 5: RPC retorna null/undefined → deve lançar 'Token inválido ou já utilizado.'
  </behavior>
  <action>
Criar o arquivo `utils/__tests__/tokenExpiry.test.ts` com testes que validam a lógica de processamento da resposta do RPC. Os testes devem testar a função de validação de forma isolada (sem chamar Supabase de verdade).

Criar o arquivo com o seguinte conteúdo:

```typescript
/**
 * Testes unitários para a lógica de validação de token de convite (AUTH-01)
 * Cobre o processamento da resposta do RPC validate_invite_token
 */

interface TokenValidation {
  id: string;
  codigo: string;
  municipio: string;
  role: string;
  valido: boolean;
  motivo: string;
}

/**
 * Extrai a lógica de validação do register.tsx para teste isolado.
 * Esta função replica exatamente o que register.tsx faz com a resposta do RPC.
 */
function processTokenValidation(
  tokenValidation: TokenValidation | null,
  validationError: Error | null,
): { municipio: string; role: string } {
  if (validationError || !tokenValidation) {
    throw new Error('Token inválido ou já utilizado.');
  }
  if (!tokenValidation.valido) {
    throw new Error(tokenValidation.motivo);
  }
  return { municipio: tokenValidation.municipio, role: tokenValidation.role };
}

describe('processTokenValidation (AUTH-01)', () => {
  it('aceita token válido e retorna municipio e role', () => {
    const result = processTokenValidation(
      { id: 'uuid-1', codigo: 'ABCD-EFGH-IJKL', municipio: 'São Paulo', role: 'agent', valido: true, motivo: 'ok' },
      null,
    );
    expect(result.municipio).toBe('São Paulo');
    expect(result.role).toBe('agent');
  });

  it('lança erro com motivo quando token expirado', () => {
    expect(() => processTokenValidation(
      { id: 'uuid-2', codigo: 'ABCD-EFGH-IJKL', municipio: 'SP', role: 'agent', valido: false, motivo: 'Token expirado. Solicite um novo ao administrador.' },
      null,
    )).toThrow('Token expirado. Solicite um novo ao administrador.');
  });

  it('lança erro com motivo quando token já utilizado', () => {
    expect(() => processTokenValidation(
      { id: 'uuid-3', codigo: 'ABCD-EFGH-IJKL', municipio: 'SP', role: 'agent', valido: false, motivo: 'Token já utilizado.' },
      null,
    )).toThrow('Token já utilizado.');
  });

  it('lança erro genérico quando RPC retorna erro', () => {
    expect(() => processTokenValidation(
      null,
      new Error('relation "invite_tokens" does not exist'),
    )).toThrow('Token inválido ou já utilizado.');
  });

  it('lança erro genérico quando RPC retorna null', () => {
    expect(() => processTokenValidation(null, null)).toThrow('Token inválido ou já utilizado.');
  });
});
```
  </action>
  <verify>
    <automated>npm test -- utils/__tests__/tokenExpiry.test.ts --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `utils/__tests__/tokenExpiry.test.ts` existe
    - `npm test -- utils/__tests__/tokenExpiry.test.ts` executa e passa (todos os 5 testes verdes)
    - O arquivo contém `describe('processTokenValidation (AUTH-01)'`
    - O arquivo contém os 5 casos de teste: aceita válido, expirado, já utilizado, erro RPC, null
  </acceptance_criteria>
  <done>5 testes verdes cobrindo os cenários de resposta do RPC validate_invite_token.</done>
</task>

<task type="auto">
  <name>Task 2: Substituir validação client-side por RPC server-side em register.tsx</name>
  <files>app/(auth)/register.tsx</files>
  <read_first>
    - app/(auth)/register.tsx (ler completo — especialmente handleRegister, linhas 44-141)
    - .planning/phases/06-mapa-autentica-o/06-RESEARCH.md (seção "Pattern 3" e "Code Examples — Recommended: Server-Side Token Validation")
  </read_first>
  <action>
Localizar e substituir o bloco de validação do token em `handleRegister` (linhas 69-83 aprox.).

**REMOVER este bloco:**
```typescript
// 1. Validar token de convite — normalizar removendo espaços e hífens (formato XXXX-XXXX-XXXX)
const codigoNorm = token.trim().toUpperCase().replace(/[\s-]/g, '');
const { data: tokenData, error: tokenError } = await supabase
  .from('invite_tokens')
  .select('id, codigo, expiraEm, municipio, role, usado')
  .eq('codigo', codigoNorm)
  .eq('usado', false)
  .single();

if (tokenError || !tokenData) throw new Error('Token inválido ou já utilizado.');

// Verificar expiração
if (tokenData.expiraEm && new Date(tokenData.expiraEm) < new Date()) {
  throw new Error('Token expirado. Solicite um novo ao administrador.');
}
```

**SUBSTITUIR por:**
```typescript
// 1. Validar token de convite via RPC server-side (evita bug de fuso horário — AUTH-01)
const codigoNorm = token.trim().toUpperCase().replace(/[\s-]/g, '');
const { data: tokenValidation, error: validationError } = await supabase
  .rpc('validate_invite_token', { p_codigo: codigoNorm })
  .single();

if (validationError || !tokenValidation) throw new Error('Token inválido ou já utilizado.');
if (!tokenValidation.valido) throw new Error(tokenValidation.motivo);

// tokenData é alias para tokenValidation — mantém compatibilidade com código downstream
const tokenData = tokenValidation;
```

Manter absolutamente inalterado tudo a partir do comentário `// Verificar domínio de email autorizado pelo município` até o final de `handleRegister`. O código de criação de conta, insert em users e delete do token permanece idêntico.

Verificar que as propriedades `tokenData.municipio` e `tokenData.role` ainda são acessadas corretamente — elas estão disponíveis no objeto `tokenValidation` retornado pelo RPC.
  </action>
  <verify>
    <automated>npm test -- --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - app/(auth)/register.tsx contém `rpc('validate_invite_token', { p_codigo: codigoNorm })`
    - app/(auth)/register.tsx NÃO contém `new Date(tokenData.expiraEm) < new Date()` (linha bugada removida)
    - app/(auth)/register.tsx NÃO contém `.from('invite_tokens').select('id, codigo, expiraEm` (query antiga removida)
    - app/(auth)/register.tsx contém `if (!tokenValidation.valido) throw new Error(tokenValidation.motivo)`
    - app/(auth)/register.tsx ainda contém `supabase.rpc('check_email_domain'` (verificação de domínio mantida)
    - app/(auth)/register.tsx ainda contém `supabase.auth.signUp` (criação de conta mantida)
    - app/(auth)/register.tsx ainda contém `supabase.from('invite_tokens').delete()` (delete do token mantido)
    - `npm test -- --passWithNoTests` finaliza com código 0
  </acceptance_criteria>
  <done>register.tsx usa RPC server-side para validação de token; comparação de data com now() do PostgreSQL elimina bug de fuso horário.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Aplicar SQL do RPC no Supabase e verificar fluxo de token</name>
  <what-built>
    - utils/__tests__/tokenExpiry.test.ts com 5 testes cobrindo validação via RPC
    - app/(auth)/register.tsx usando supabase.rpc('validate_invite_token') em vez de comparação client-side
  </what-built>
  <how-to-verify>
    **Passo 1 — Aplicar SQL no Supabase Dashboard (obrigatório antes de testar o app):**

    Abrir Supabase Dashboard → SQL Editor → executar o SQL abaixo:

    ```sql
    CREATE OR REPLACE FUNCTION validate_invite_token(p_codigo TEXT)
    RETURNS TABLE (
      id UUID,
      codigo TEXT,
      municipio TEXT,
      role TEXT,
      valido BOOLEAN,
      motivo TEXT
    ) AS $$
    BEGIN
      RETURN QUERY
      SELECT
        t.id,
        t.codigo,
        t.municipio,
        t.role,
        CASE
          WHEN t.usado THEN FALSE
          WHEN t."expiraEm" IS NOT NULL AND t."expiraEm" < now() THEN FALSE
          ELSE TRUE
        END AS valido,
        CASE
          WHEN t.usado THEN 'Token já utilizado.'
          WHEN t."expiraEm" IS NOT NULL AND t."expiraEm" < now() THEN 'Token expirado. Solicite um novo ao administrador.'
          ELSE 'ok'
        END AS motivo
      FROM invite_tokens t
      WHERE t.codigo = p_codigo AND t.usado = false
      LIMIT 1;
    END;
    $$ LANGUAGE plpgsql SECURITY DEFINER;
    ```

    **Passo 2 (opcional mas recomendado) — Verificar tipo da coluna expiraEm:**

    No Supabase SQL Editor:
    ```sql
    SELECT column_name, data_type
    FROM information_schema.columns
    WHERE table_name = 'invite_tokens' AND column_name = 'expiraEm';
    ```

    Se retornar `timestamp without time zone` (TIMESTAMP), executar:
    ```sql
    ALTER TABLE invite_tokens ALTER COLUMN "expiraEm" TYPE TIMESTAMPTZ USING "expiraEm" AT TIME ZONE 'UTC';
    ```

    **Passo 3 — Testar no app:**
    1. Logar como admin no app
    2. Ir em "Gerar Token" → selecionar perfil "Agente" → duração "24h" → gerar
    3. Anotar o código gerado (ex: ABCD-EFGH-IJKL)
    4. Sair do app ou abrir em outro device
    5. Ir em "Cadastrar" → inserir o código recém gerado → preencher nome/email/senha → confirmar
    6. ESPERADO: registro concluído sem erro "Token expirado"
  </how-to-verify>
  <resume-signal>Digite "aprovado" se o fluxo de registro com token funcionar, ou descreva o erro exato recebido</resume-signal>
</task>

</tasks>

<verification>
- `npm test -- utils/__tests__/tokenExpiry.test.ts` — 5 testes verdes
- `npm test -- --passWithNoTests` — suite completa passa
- `app/(auth)/register.tsx` usa `rpc('validate_invite_token')` (grep confirma)
- `app/(auth)/register.tsx` não contém a comparação bugada (grep confirma ausência)
- SQL do RPC aplicado no Supabase (verificação manual)
- Fluxo de registro funcional no app (verificação manual)
</verification>

<success_criteria>
- AUTH-01: Token recém-criado aceito imediatamente pelo fluxo de registro sem erro "Token expirado"
- Token expirado genuinamente retorna mensagem correta em português via motivo do RPC
- 5 testes unitários verdes cobrindo todos os cenários de resposta do RPC
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/06-mapa-autentica-o/06-02-SUMMARY.md`
</output>
