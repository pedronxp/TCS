---
phase: 06-mapa-autentica-o
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(panel)/master/municipios.tsx
autonomous: false
requirements:
  - AUTH-02

must_haves:
  truths:
    - "Master admin cadastra um novo município e o registro aparece na lista sem mensagem de erro"
    - "Se o insert falhar por RLS, o erro exibido está em português com orientação clara"
    - "Se o insert falhar por RLS com código 42501, a mensagem diz 'Permissão negada. Verifique se você tem perfil de master admin.'"
  artifacts:
    - path: "app/(panel)/master/municipios.tsx"
      provides: "Tela de gestão de municípios com tratamento de erro RLS em português"
      contains: "42501"
  key_links:
    - from: "app/(panel)/master/municipios.tsx (criarMunicipio)"
      to: "Alert.alert com mensagem específica para código 42501"
      via: "e?.code === '42501'"
      pattern: "42501"
---

<objective>
Corrige AUTH-02: criação de municípios falha silenciosamente ou exibe mensagem de erro em inglês/genérica quando bloqueada por RLS.

Duas partes:

Parte 1 (código — autônoma): O `catch` em `criarMunicipio()` exibe `e.message || 'Não foi possível criar o município.'`. Quando o Supabase bloqueia por RLS, retorna `error.code === '42501'` com mensagem em inglês ("new row violates row-level security policy"). Adicionar verificação do código 42501 para exibir mensagem em português e também fazer o mesmo em `adicionarDominio()`.

Parte 2 (Supabase SQL — requer humano): As políticas RLS para `master_admin` na tabela `municipios` precisam existir. O executor aplica o SQL no Supabase Dashboard. O código já está correto — a falha é de configuração de banco de dados.

Purpose: Master admin precisa cadastrar municípios para configurar o sistema antes de qualquer agente poder usar o app.
Output: `municipios.tsx` com tratamento de erro RLS em português + RLS policies aplicadas via checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-mapa-autentica-o/06-RESEARCH.md

<interfaces>
<!-- Código atual em municipios.tsx que será modificado -->

criarMunicipio() catch atual (linha 174):
```typescript
} catch (e: any) {
  Alert.alert('Erro', e.message || 'Não foi possível criar o município.');
  logger.error('system', 'Erro criar município', { erro: (e as any)?.message || JSON.stringify(e) });
}
```

adicionarDominio() catch atual (linha 148):
```typescript
} catch (e) {
  Alert.alert('Erro', 'Não foi possível salvar.');
  logger.error('system', 'Erro salvar domínio', { erro: (e as any)?.message || JSON.stringify(e) });
}
```

Supabase RLS error shape:
- `error.code === '42501'` → "new row violates row-level security policy"
- `error.code === '23505'` → unique constraint violation (nome duplicado)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar tratamento de erro RLS (código 42501) em criarMunicipio e adicionarDominio</name>
  <files>app/(panel)/master/municipios.tsx</files>
  <read_first>
    - app/(panel)/master/municipios.tsx (ler completo — especialmente criarMunicipio linhas 156-180 e adicionarDominio linhas 125-154)
    - .planning/phases/06-mapa-autentica-o/06-RESEARCH.md (seção "Pitfall 3" e "Code Examples — RLS Error Handling")
  </read_first>
  <action>
**Mudança 1 — criarMunicipio() catch:**

Localizar o bloco catch de `criarMunicipio` (linha ~174):
```typescript
} catch (e: any) {
  Alert.alert('Erro', e.message || 'Não foi possível criar o município.');
  logger.error('system', 'Erro criar município', { erro: (e as any)?.message || JSON.stringify(e) });
}
```

Substituir por:
```typescript
} catch (e: any) {
  const msg = e?.code === '42501'
    ? 'Permissão negada. Verifique se você tem perfil de master admin.'
    : e?.code === '23505'
    ? 'Este município já está cadastrado.'
    : e?.message || 'Não foi possível criar o município.';
  Alert.alert('Erro', msg);
  logger.error('system', 'Erro criar município', { erro: e?.message || JSON.stringify(e), code: e?.code });
}
```

**Mudança 2 — adicionarDominio() catch:**

Localizar o bloco catch de `adicionarDominio` (linha ~148):
```typescript
} catch (e) {
  Alert.alert('Erro', 'Não foi possível salvar.');
  logger.error('system', 'Erro salvar domínio', { erro: (e as any)?.message || JSON.stringify(e) });
}
```

Substituir por:
```typescript
} catch (e: any) {
  const msg = e?.code === '42501'
    ? 'Permissão negada. Verifique se você tem perfil de master admin.'
    : e?.message || 'Não foi possível salvar o domínio.';
  Alert.alert('Erro', msg);
  logger.error('system', 'Erro salvar domínio', { erro: e?.message || JSON.stringify(e), code: e?.code });
}
```

**Mudança 3 — removerDominio() catch interno:**

Localizar o catch dentro do `Alert.alert` de `removerDominio` (linha ~195):
```typescript
} catch (e) {
  Alert.alert('Erro', 'Não foi possível remover.');
}
```

Substituir por:
```typescript
} catch (e: any) {
  const msg = e?.code === '42501'
    ? 'Permissão negada. Verifique se você tem perfil de master admin.'
    : 'Não foi possível remover o domínio.';
  Alert.alert('Erro', msg);
}
```
  </action>
  <verify>
    <automated>npm test -- --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - app/(panel)/master/municipios.tsx contém `e?.code === '42501'` (pelo menos 2 ocorrências: criarMunicipio e adicionarDominio)
    - app/(panel)/master/municipios.tsx contém `'Permissão negada. Verifique se você tem perfil de master admin.'`
    - app/(panel)/master/municipios.tsx contém `e?.code === '23505'` (tratamento de duplicata em criarMunicipio)
    - app/(panel)/master/municipios.tsx contém `code: e?.code` no logger.error de criarMunicipio (para debug)
    - `npm test -- --passWithNoTests` finaliza com código 0
  </acceptance_criteria>
  <done>Erros RLS exibem mensagem em português com orientação; erros de constraint de unicidade exibem mensagem descritiva; código de erro logado para diagnóstico.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Aplicar RLS policies no Supabase e testar criação de município</name>
  <what-built>
    - app/(panel)/master/municipios.tsx com tratamento de erro RLS em português (código 42501)
    - Erro de duplicata (23505) também traduzido
  </what-built>
  <how-to-verify>
    **Passo 1 — Verificar RLS atual na tabela municipios:**

    No Supabase Dashboard → Table Editor → tabela `municipios` → aba "Policies"
    OU via SQL Editor:
    ```sql
    SELECT policyname, cmd, qual, with_check
    FROM pg_policies
    WHERE tablename = 'municipios';
    ```

    **Passo 2 — Se não existir policy de INSERT para master_admin, aplicar:**

    No Supabase SQL Editor → executar:
    ```sql
    -- Leitura: todos autenticados podem ler municípios
    CREATE POLICY "authenticated_read_municipios"
    ON municipios
    FOR SELECT
    TO authenticated
    USING (true);

    -- Insert: apenas master_admin
    CREATE POLICY "master_admin_insert_municipios"
    ON municipios
    FOR INSERT
    TO authenticated
    WITH CHECK (
      (SELECT role FROM users WHERE uid = auth.uid()) = 'master_admin'
    );

    -- Update: apenas master_admin (para upsert de domínios)
    CREATE POLICY "master_admin_update_municipios"
    ON municipios
    FOR UPDATE
    TO authenticated
    USING (
      (SELECT role FROM users WHERE uid = auth.uid()) = 'master_admin'
    );
    ```

    ATENÇÃO: Se as policies já existirem, o SQL vai retornar erro "policy already exists" — isso é OK, não precisa re-aplicar.

    **Passo 3 — Testar no app:**
    1. Logar como master_admin no app
    2. Navegar para Master → Municípios
    3. Clicar no botão "+" para criar novo município
    4. Digitar um nome (ex: "Campinas Teste") → confirmar
    5. ESPERADO: município aparece na lista sem Alert de erro
    6. Tentar criar o mesmo município novamente
    7. ESPERADO: Alert "Este município já está cadastrado." (sem mensagem em inglês)

    **Diagnóstico adicional se falhar:**
    - Se aparece "Permissão negada. Verifique se você tem perfil de master admin." → RLS não está configurado ou o user não tem role = 'master_admin' na tabela users
    - Verificar: `SELECT role FROM users WHERE uid = '<uid do usuário logado>';`
  </how-to-verify>
  <resume-signal>Digite "aprovado" se município for criado com sucesso, ou descreva o erro e o resultado da query de diagnóstico</resume-signal>
</task>

</tasks>

<verification>
- `npm test -- --passWithNoTests` passa
- `app/(panel)/master/municipios.tsx` contém tratamento para código 42501 (grep confirma)
- RLS policies existem no Supabase (verificação manual via dashboard)
- Criação de município funcional no app como master_admin (verificação manual)
</verification>

<success_criteria>
- AUTH-02: Master admin cria município com sucesso e registro aparece na lista
- Erros de RLS exibem mensagem em português com orientação clara (não mensagem em inglês genérica)
- Erros de duplicata exibem "Este município já está cadastrado."
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/06-mapa-autentica-o/06-03-SUMMARY.md`
</output>
