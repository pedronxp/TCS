---
phase: "04"
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified: 
  - "app/(panel)/supervisor/equipe.tsx"
  - "app/(panel)/supervisor/agente.tsx"
  - "app/(panel)/admin/form-editor.tsx"
  - "app/(panel)/admin/editor-perguntas.tsx"
autonomous: true
requirements: 
  - "PH4-07"
  - "PH4-08"
must_haves:
  truths:
    - "Supervisor team and agent views use design system components for loading and empty states"
    - "Admin form editors use consistent buttons and states"
  artifacts:
    - path: "app/(panel)/supervisor/equipe.tsx"
      provides: "Team list with empty state handling"
    - path: "app/(panel)/admin/form-editor.tsx"
      provides: "Form editor list with standard buttons"
  key_links:
    - from: "equipe.tsx"
      to: "EmptyState component"
      via: "conditional rendering when list is empty"
---

<objective>
Finalize the visual hierarchy migration by applying the design system components to the supervisor's team management screens and the admin's form builders.

Purpose: Polish the remaining secondary screens to ensure the entire application respects the unified design language without functional regressions.
Output: Consistent empty, error, and loading states across team views and form editors.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply Design System to Supervisor Views</name>
  <files>app/(panel)/supervisor/equipe.tsx, app/(panel)/supervisor/agente.tsx</files>
  <action>
    For both files:
    1. Import `LoadingState`, `EmptyState`, and `ErrorState` from `../../../components/ui/`.
    2. Replace `<ActivityIndicator>` with `<LoadingState />`.
    3. Ensure any silent `catch` during data fetch triggers an `<ErrorState />`.
    4. In `equipe.tsx`: Replace inline empty texts with `<EmptyState icon="users" title="Equipe vazia" message="Nenhum agente ativo no município." />`. Update agent cards to use `borderRadius: 16` and `borderColor: theme.cardBorder`.
    5. In `agente.tsx`: If the attribution history is empty, show `<EmptyState icon="clipboard" title="Sem atribuições" message="Este agente não tem atribuições registradas." />`.
  </action>
  <verify>
    <automated>grep -q "EmptyState" app/\(panel\)/supervisor/equipe.tsx</automated>
  </verify>
  <done>Supervisor team and agent screens handle empty arrays and errors gracefully.</done>
</task>

<task type="auto">
  <name>Task 2: Apply Design System to Form Editors</name>
  <files>app/(panel)/admin/form-editor.tsx, app/(panel)/admin/editor-perguntas.tsx</files>
  <action>
    For both files:
    1. Import `LoadingState`, `EmptyState`, and `ErrorState`.
    2. Replace native loading indicators with `<LoadingState />` and silent errors with `<ErrorState />`.
    3. In `form-editor.tsx`: If the forms list is empty, show `<EmptyState icon="edit" title="Nenhum formulário" message="Crie o primeiro formulário clicando no botão abaixo." />`. Try to use `<Button variant="primary">` for the "Novo Formulário" action if straightforward, else match border radius/padding manually.
    4. In `editor-perguntas.tsx`: Show `<EmptyState icon="list" title="Nenhuma pergunta" message="Adicione perguntas ao formulário." />` for empty lists. Ensure question cards have `borderRadius: 14` and `borderColor: theme.cardBorder`.
  </action>
  <verify>
    <automated>grep -q "EmptyState" app/\(panel\)/admin/form-editor.tsx</automated>
  </verify>
  <done>Form editors are visually consistent with the rest of the application.</done>
</task>

</tasks>

<verification>
Ensure the supervisor views and admin form editor screens don't break existing navigation or logic and successfully display `<EmptyState>` when their respective data lists are empty.
</verification>

<success_criteria>
All 4 screens fully transitioned to use the custom UI components for their structural states (loading, empty, error).
</success_criteria>

<output>
After completion, create `.planning/phases/04-ui-admin-supervisor-master/04-04-SUMMARY.md`
</output>