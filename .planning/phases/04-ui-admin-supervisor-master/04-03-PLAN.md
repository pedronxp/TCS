---
phase: "04"
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified: 
  - "app/(panel)/master/municipios.tsx"
  - "app/(panel)/admin/usuarios.tsx"
  - "app/(panel)/admin/tokens.tsx"
  - "app/(panel)/admin/gerar-token.tsx"
  - "app/(panel)/admin/estatisticas.tsx"
autonomous: true
requirements: 
  - "PH4-05"
  - "PH4-06"
must_haves:
  truths:
    - "Municipios list loads fast using RPC instead of unfiltered queries"
    - "Admin list screens display appropriate EmptyStates and ErrorStates"
    - "Generate token form uses design system components"
  artifacts:
    - path: "app/(panel)/master/municipios.tsx"
      provides: "Optimized city stats fetching"
    - path: "app/(panel)/admin/usuarios.tsx"
      provides: "User list with design system components"
    - path: "app/(panel)/admin/estatisticas.tsx"
      provides: "Stats view with design system components"
  key_links:
    - from: "municipios.tsx"
      to: "get_municipios_stats RPC"
      via: "supabase.rpc call"
---

<objective>
Optimize the performance of the heaviest screen in the app (municipios) and apply the Design System's feedback components to secondary Admin screens to eliminate silent states.

Purpose: Prevent client-side memory overload by pushing aggregation to the database, and unify the visual language of the admin tools.
Output: Optimized RPC query in master/municipios and polished admin screens.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Optimize Municipios with RPC</name>
  <files>app/(panel)/master/municipios.tsx</files>
  <action>
    1. Replace the `supabase.from('vistorias').select(...)` query (without `.limit()`) with a call to `supabase.rpc('get_municipios_stats')`. Add a comment with the SQL to create this RPC if it doesn't exist (see research notes).
    2. Remove the separate query to `users` since the RPC groups this data. Keep the query to `municipios`.
    3. Update the mapping logic to use `statsRes.data` (which returns `{ municipio, total_vistorias, alto_risco, total_agentes }`) instead of manually counting records in the client.
    4. Add an `ErrorState` handling pattern for the `catch` block (following the pattern established in Plan 01).
  </action>
  <verify>
    <automated>grep -q "rpc('get_municipios_stats')" app/\(panel\)/master/municipios.tsx</automated>
  </verify>
  <done>Screen loads immediately by fetching pre-aggregated data instead of thousands of raw rows.</done>
</task>

<task type="auto">
  <name>Task 2: Apply Design System to Users and Tokens Lists</name>
  <files>app/(panel)/admin/usuarios.tsx, app/(panel)/admin/tokens.tsx</files>
  <action>
    For both files:
    1. Import `LoadingState`, `EmptyState`, `ErrorState`, and `Badge` from `../../../components/ui/`.
    2. Replace inline `<ActivityIndicator>` with `<LoadingState />`.
    3. Replace inline empty states with `<EmptyState icon="..." title="..." message="..." />` (use appropriate icons like "users" or "key").
    4. Ensure any silent `catch` blocks display `<ErrorState onRetry={carregar} />`.
    5. In `usuarios.tsx`, ensure user cards use standard border styles (`borderRadius: 16`, `borderWidth: 1`, `borderColor: theme.cardBorder`).
    6. In `tokens.tsx`, replace inline active/expired pills with the `Badge` component.
  </action>
  <verify>
    <automated>grep -q "LoadingState\|EmptyState\|ErrorState" app/\(panel\)/admin/usuarios.tsx</automated>
  </verify>
  <done>Lists have robust feedback states and visually match the new design system.</done>
</task>

<task type="auto">
  <name>Task 3: Apply Design System to Token Form and Stats</name>
  <files>app/(panel)/admin/gerar-token.tsx, app/(panel)/admin/estatisticas.tsx</files>
  <action>
    In `gerar-token.tsx`:
    1. Import the `Button` component from `../../../components/ui/Button`.
    2. Replace the custom submit `TouchableOpacity` with `<Button variant="primary" loading={salvando} onPress={handleGerar} ... />` to provide built-in visual feedback.

    In `estatisticas.tsx`:
    1. Import `LoadingState`, `EmptyState`, and `ErrorState`.
    2. Replace `<ActivityIndicator>` with `<LoadingState />`.
    3. If there's an error during fetch, show `<ErrorState />`.
    4. If data is successfully loaded but empty, show `<EmptyState icon="bar-chart-2" title="Sem dados" message="Nenhuma estatística disponível." />`.
  </action>
  <verify>
    <automated>grep -q "EmptyState" app/\(panel\)/admin/estatisticas.tsx && grep -q "<Button" app/\(panel\)/admin/gerar-token.tsx</automated>
  </verify>
  <done>Token generator form and stats dashboard show correct interactive and empty states.</done>
</task>

</tasks>

<verification>
Ensure the app no longer downloads all vistorias at once in the master panel. Ensure all touched admin screens render correctly during loading, empty, and error conditions.
</verification>

<success_criteria>
No unbounded queries in `municipios.tsx`. Complete coverage of Empty/Error states in target admin views.
</success_criteria>

<output>
After completion, create `.planning/phases/04-ui-admin-supervisor-master/04-03-SUMMARY.md`
</output>