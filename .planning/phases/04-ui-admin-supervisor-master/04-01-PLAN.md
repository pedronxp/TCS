---
phase: "04"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: 
  - "app/(panel)/admin/index.tsx"
  - "app/(panel)/supervisor/index.tsx"
  - "app/(panel)/master/index.tsx"
  - "app/(panel)/master/logs.tsx"
autonomous: true
requirements: 
  - "PH4-01"
must_haves:
  truths:
    - "Admin dashboard shows error message when metrics fail to load"
    - "Supervisor dashboard shows error message when lists fail to load"
    - "Master dashboard and logs show error message when data fetch fails"
  artifacts:
    - path: "app/(panel)/admin/index.tsx"
      provides: "Admin Dashboard with error handling"
    - path: "app/(panel)/supervisor/index.tsx"
      provides: "Supervisor Dashboard with error handling"
    - path: "app/(panel)/master/index.tsx"
      provides: "Master Dashboard with error handling"
    - path: "app/(panel)/master/logs.tsx"
      provides: "Master Logs with error handling"
  key_links:
    - from: "catch block in carregar()"
      to: "ErrorState component"
      via: "erro state variable"
---

<objective>
Eliminate silent failures by adding explicit ErrorStates to all main dashboard screens.

Purpose: Prevent users from seeing empty screens with zero context when data fetching fails.
Output: Admin, Supervisor, and Master dashboards updated with ErrorState components.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ErrorState to Admin and Supervisor Dashboards</name>
  <files>app/(panel)/admin/index.tsx, app/(panel)/supervisor/index.tsx</files>
  <action>
    For both `admin/index.tsx` and `supervisor/index.tsx`:
    1. Import `ErrorState` from `../../../components/ui/ErrorState`.
    2. Add `const [erro, setErro] = useState(false)` to state declarations.
    3. Reset `setErro(false)` at the start of the `carregar()` function (before the `try` block).
    4. In the `catch` block of `carregar()`, call `setErro(true)` after logging the error.
    5. In `admin/index.tsx`: Conditionally render `ErrorState` (title "Erro ao carregar métricas", message "Não foi possível buscar os dados do município. Puxe para atualizar.") in place of the KPI grid if `erro` is true and `kpis.length === 0`. Do the same for "Atividade Recente" if empty.
    6. In `supervisor/index.tsx`: Conditionally render `ErrorState` (title "Erro ao carregar dados", message "Falha ao buscar vistorias e atribuições. Puxe para atualizar.") before the main `ScrollView` if `erro && !loading`.
  </action>
  <verify>
    <automated>grep -q "ErrorState" app/\(panel\)/admin/index.tsx && grep -q "setErro(true)" app/\(panel\)/supervisor/index.tsx</automated>
  </verify>
  <done>Dashboards handle network or API errors visually instead of silently.</done>
</task>

<task type="auto">
  <name>Task 2: Add ErrorState to Master Index and Master Logs</name>
  <files>app/(panel)/master/index.tsx, app/(panel)/master/logs.tsx</files>
  <action>
    For `master/index.tsx`:
    1. Import `ErrorState` from `../../../components/ui/ErrorState`.
    2. Add `const [erro, setErro] = useState(false)`.
    3. Reset to false at the start of `carregar()`, set to true in the `catch` block.
    4. Render `ErrorState` before the `ScrollView` if `erro && !loading && stats.totalVistorias === 0`.

    For `master/logs.tsx`:
    1. Import `ErrorState` from `../../../components/ui/ErrorState`.
    2. Add `const [erroAuditoria, setErroAuditoria] = useState(false)` (or similar for system logs if applicable).
    3. Set `erroAuditoria` to true in the catch block of `loadAuditLogs`.
    4. Render `ErrorState` in the audit tab if `auditLoading === false`, `auditLogs.length === 0` and `erroAuditoria === true` instead of an empty view.
  </action>
  <verify>
    <automated>grep -q "ErrorState" app/\(panel\)/master/index.tsx && grep -q "ErrorState" app/\(panel\)/master/logs.tsx</automated>
  </verify>
  <done>Master screens show error states when API requests fail.</done>
</task>

</tasks>

<verification>
All main dashboard files must import and render `<ErrorState>` from the UI components.
</verification>

<success_criteria>
Zero screens with silent failures on load data error. Admin, Supervisor, and Master indexes correctly display ErrorState.
</success_criteria>

<output>
After completion, create `.planning/phases/04-ui-admin-supervisor-master/04-01-SUMMARY.md`
</output>