---
phase: "04"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified: 
  - "app/(panel)/supervisor/atribuicao.tsx"
  - "app/(panel)/admin/risco-config.tsx"
  - "app/(panel)/admin/logs.tsx"
autonomous: true
requirements: 
  - "PH4-02"
  - "PH4-03"
  - "PH4-04"
must_haves:
  truths:
    - "Creating an attribution triggers a push notification"
    - "Risk configuration cache correctly expires after 24 hours"
    - "Logs can be exported as a CSV file"
  artifacts:
    - path: "app/(panel)/supervisor/atribuicao.tsx"
      provides: "Attribution creation with notification"
    - path: "app/(panel)/admin/risco-config.tsx"
      provides: "Risk configuration with 24h cache TTL"
    - path: "app/(panel)/admin/logs.tsx"
      provides: "Logs export to CSV functionality"
  key_links:
    - from: "handleSalvar in atribuicao.tsx"
      to: "notificarNovaAtribuicao in NotificationService"
      via: "function call on success"
    - from: "AsyncStorage read in risco-config.tsx"
      to: "cached_at timestamp"
      via: "TTL validation before using cache"
---

<objective>
Implement missing functionalities in admin and supervisor screens: real push notifications for new attributions, cache expiration for risk settings, and CSV export for logs.

Purpose: Fix a fake promise to the user (notification alert when none is sent), ensure critical settings don't get permanently stuck in cache, and enable admin log auditing.
Output: attribution with notification, TTL cache for risk config, and export button in logs.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Push Notification to Attribution Creation</name>
  <files>app/(panel)/supervisor/atribuicao.tsx</files>
  <action>
    1. Import `notificarNovaAtribuicao` from `../../../services/NotificationService`.
    2. In `handleSalvar`, after the Supabase insert succeeds (and before the `Alert.alert`), call `await notificarNovaAtribuicao(enderecoCompleto, prioridade)` inside a `try/catch` block (so a notification failure doesn't crash the attribution flow).
    3. Update the success `Alert.alert` text to remove the false promise "O agente foi notificado..." and instead just say "Missão atribuída com sucesso!".
    4. Add `maxLength={500}` to the `observacoes` TextInput and add a character counter text `<Text>{observacoes.length}/500</Text>` below it.
  </action>
  <verify>
    <automated>grep -q "notificarNovaAtribuicao" app/\(panel\)/supervisor/atribuicao.tsx</automated>
  </verify>
  <done>Attribution creation genuinely attempts to send a notification and limits notes to 500 characters.</done>
</task>

<task type="auto">
  <name>Task 2: Implement 24h TTL in Risco Config Cache</name>
  <files>app/(panel)/admin/risco-config.tsx</files>
  <action>
    1. Change the storage key to `@risco_config_v2` to invalidate old caches.
    2. Define `CACHE_TTL_MS = 24 * 60 * 60 * 1000`.
    3. Update the storage structure to wrap the config: `{ config, cached_at: Date.now() }`.
    4. Update the `AsyncStorage.setItem` calls in `loadConfig` (when fetching from cloud) and `salvar` to include `cached_at`.
    5. Update the fallback read logic: parse the JSON, check if `Date.now() - cache.cached_at > CACHE_TTL_MS`. If expired, do NOT set the local config (it will fall back to `DEFAULT_CONFIG`).
    6. Show cache expiration info in the UI if `syncStatus === 'local'`: e.g., "Cache local · expira em Xh".
  </action>
  <verify>
    <automated>grep -q "cached_at" app/\(panel\)/admin/risco-config.tsx</automated>
  </verify>
  <done>Configuration cache strictly respects a 24-hour expiration rule.</done>
</task>

<task type="auto">
  <name>Task 3: Implement CSV Export in Logs</name>
  <files>app/(panel)/admin/logs.tsx</files>
  <action>
    1. Ensure `Share` is imported from `react-native`.
    2. Create a function `gerarCsvSistema(entries)` that returns a CSV string: "Data,Nível,Categoria,Mensagem" with rows. Ensure text is properly escaped/formatted (replace commas and newlines).
    3. Create a function `gerarCsvAuditoria(entries)` returning "Data,Ação,Ator,Alvo,Município" and mapping rows.
    4. Create `handleExportar` that generates the CSV for the `activeTab` and calls `Share.share({ message: csv, title: "..." })`. Wrap in try/catch with fallback Alert.
    5. Add an export button (`<TouchableOpacity onPress={handleExportar}>`) to the header of the logs screen, with an appropriate icon (e.g., Feather `download`).
  </action>
  <verify>
    <automated>grep -q "Share.share" app/\(panel\)/admin/logs.tsx</automated>
  </verify>
  <done>Admins can download the system and audit logs as CSV files.</done>
</task>

</tasks>

<verification>
Ensure all functional gaps are filled without introducing new dependencies.
</verification>

<success_criteria>
Push notifications work, configs expire automatically, logs can be exported.
</success_criteria>

<output>
After completion, create `.planning/phases/04-ui-admin-supervisor-master/04-02-SUMMARY.md`
</output>