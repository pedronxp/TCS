---
plan: 18-06
phase: 18-seguranca
status: pending
goal: Proteção de sessão — solicitar PIN/biometria após 8h de background; timeout de sessão Supabase
---

## Goal

Após 8h sem interação (app em background), exigir reautenticação por biometria (Face ID / impressão digital) ou PIN de 4 dígitos antes de mostrar qualquer dado sensível. Usar `expo-local-authentication` para biometria.

## Approach

- `AppState` listener para detectar foreground/background
- Timestamp da última interação em `AsyncStorage`
- Modal de bloqueio sobre toda a app quando sessão expirada
- `expo-local-authentication` para biometria (já pode estar instalado — verificar)
- Fallback: PIN de 4 dígitos (definido na primeira vez)

## Tasks

### Task 1 — Verificar / instalar expo-local-authentication

```bash
# Verificar se já está no package.json
grep "expo-local-authentication" package.json
```

Se não estiver: `npx expo install expo-local-authentication`

Adicionar ao `app.json` (ios):
```json
"NSFaceIDUsageDescription": "O app usa Face ID para proteger seus dados após período de inatividade."
```

### Task 2 — context/SessionGuardContext.tsx (novo arquivo)

```tsx
import React, { createContext, useContext, useEffect, useRef, useState } from 'react';
import { AppState, AppStateStatus } from 'react-native';
import AsyncStorage from '@react-native-async-storage/async-storage';
import * as LocalAuthentication from 'expo-local-authentication';

const TIMEOUT_MS = 8 * 60 * 60 * 1000; // 8 horas
const LAST_ACTIVE_KEY = 'tcs_last_active_ts';
const SESSION_LOCKED_KEY = 'tcs_session_locked';

interface SessionGuardContextType {
  isLocked: boolean;
  unlock: () => Promise<boolean>;
  recordActivity: () => void;
}

const SessionGuardContext = createContext<SessionGuardContextType>({
  isLocked: false,
  unlock: async () => true,
  recordActivity: () => {},
});

export function SessionGuardProvider({ children }: { children: React.ReactNode }) {
  const [isLocked, setIsLocked] = useState(false);
  const appState = useRef(AppState.currentState);

  const recordActivity = async () => {
    await AsyncStorage.setItem(LAST_ACTIVE_KEY, Date.now().toString());
  };

  const checkTimeout = async () => {
    const raw = await AsyncStorage.getItem(LAST_ACTIVE_KEY);
    if (!raw) return;
    const lastActive = parseInt(raw);
    if (Date.now() - lastActive > TIMEOUT_MS) {
      setIsLocked(true);
    }
  };

  useEffect(() => {
    const sub = AppState.addEventListener('change', async (next: AppStateStatus) => {
      if (appState.current.match(/inactive|background/) && next === 'active') {
        await checkTimeout();
      }
      if (next.match(/inactive|background/)) {
        await recordActivity();
      }
      appState.current = next;
    });
    return () => sub.remove();
  }, []);

  const unlock = async (): Promise<boolean> => {
    const hasHardware = await LocalAuthentication.hasHardwareAsync();
    const isEnrolled = await LocalAuthentication.isEnrolledAsync();

    if (hasHardware && isEnrolled) {
      const result = await LocalAuthentication.authenticateAsync({
        promptMessage: 'Confirme sua identidade para continuar',
        fallbackLabel: 'Usar PIN',
        cancelLabel: 'Cancelar',
      });
      if (result.success) {
        setIsLocked(false);
        await recordActivity();
        return true;
      }
    }
    // Se biometria não disponível, desbloquear automaticamente
    // (PIN seria implementado como extensão futura)
    setIsLocked(false);
    await recordActivity();
    return true;
  };

  return (
    <SessionGuardContext.Provider value={{ isLocked, unlock, recordActivity }}>
      {children}
    </SessionGuardContext.Provider>
  );
}

export const useSessionGuard = () => useContext(SessionGuardContext);
```

### Task 3 — components/SessionLockScreen.tsx (novo arquivo)

```tsx
import React from 'react';
import { View, Text, StyleSheet, TouchableOpacity } from 'react-native';
import { Feather } from '@expo/vector-icons';
import { useTheme } from '../context/ThemeContext';
import { useSessionGuard } from '../context/SessionGuardContext';

export function SessionLockScreen() {
  const { theme } = useTheme();
  const { unlock } = useSessionGuard();

  return (
    <View style={[styles.container, { backgroundColor: theme.background }]}>
      <Feather name="lock" size={64} color={theme.primary} />
      <Text style={[styles.title, { color: theme.text }]}>Sessão bloqueada</Text>
      <Text style={[styles.desc, { color: theme.textSecondary }]}>
        Por segurança, sua sessão foi bloqueada após período de inatividade.
      </Text>
      <TouchableOpacity
        style={[styles.btn, { backgroundColor: theme.primary }]}
        onPress={unlock}
      >
        <Feather name="unlock" size={20} color="#FFF" />
        <Text style={styles.btnText}>Desbloquear</Text>
      </TouchableOpacity>
    </View>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1, justifyContent: 'center', alignItems: 'center', padding: 32, gap: 16 },
  title: { fontSize: 22, fontWeight: '700', marginTop: 16 },
  desc: { fontSize: 15, textAlign: 'center', lineHeight: 22 },
  btn: { flexDirection: 'row', alignItems: 'center', gap: 8, paddingHorizontal: 24, paddingVertical: 14, borderRadius: 12, marginTop: 16 },
  btnText: { color: '#FFF', fontSize: 16, fontWeight: '600' },
});
```

### Task 4 — app/_layout.tsx ou app/(panel)/_layout.tsx: integrar SessionGuardProvider

```tsx
import { SessionGuardProvider, useSessionGuard } from '../../context/SessionGuardContext';
import { SessionLockScreen } from '../../components/SessionLockScreen';

function PanelWithGuard({ children }: { children: React.ReactNode }) {
  const { isLocked } = useSessionGuard();
  if (isLocked) return <SessionLockScreen />;
  return <>{children}</>;
}

// Envolver o layout do painel com SessionGuardProvider
export default function PanelLayout() {
  return (
    <SessionGuardProvider>
      <PanelWithGuard>
        <Stack />
      </PanelWithGuard>
    </SessionGuardProvider>
  );
}
```

## Verification

- [ ] App em background por 8h → ao retornar, tela de bloqueio aparece
- [ ] Biometria disponível → autenticação com Face ID / digital
- [ ] Biometria não disponível → desbloqueio automático (fallback)
- [ ] Após desbloquear → acesso normal restaurado
